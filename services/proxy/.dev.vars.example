# Скопировать в .dev.vars (не коммитится, см. .gitignore).
# Используется только wrangler dev; production-секреты — через `wrangler secret put`.

ANTHROPIC_API_KEY=
SONIOX_API_KEY=
GLADIA_API_KEY=
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
