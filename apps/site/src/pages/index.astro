---
/**
 * Русский лендинг — дефолтная локаль, без префикса.
 *
 * Перекрывает корневой роут Starlight: файловые роуты приоритетнее
 * инжектированных интеграцией, поэтому доки остаются на своих адресах,
 * а «/» отдаёт эту страницу.
 */
import LandingPage from '../components/landing/LandingPage.astro';
---

<LandingPage lang="ru" />
