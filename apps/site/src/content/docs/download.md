---
title: Установка
description: Как поставить Wotold на macOS, что попросит приложение при первом запуске и сколько весят модели.
---

## Требования

- **macOS 14.4 или новее, Apple Silicon.** Захват системного звука использует Core Audio process tap, которого в более ранних версиях нет. Intel-маки не поддерживаются: сайдкары распознавания и генерации собраны только под arm64.
- **Свободное место** — от 2 до 7 ГБ под модели, в зависимости от пресета, плюс место под аудио записей.

Linux и Windows не поддерживаются: захват аудио и локальный движок оставлены за
trait-заглушками (ограничения R4 и R9). Это принятое ограничение MVP, а не баг.

## Установка через Homebrew

```bash
brew tap zdllucky/wotold
brew install --cask wotold
```

Обновление — `brew upgrade --cask wotold`, удаление вместе со всеми данными —
`brew uninstall --zap --cask wotold`.

Homebrew ставит карантин, и при первом запуске сработает Gatekeeper (см. ниже). Обойти это
можно флагом `--no-quarantine`, но тогда проверка пропускается целиком — решение осознанное,
поэтому оно за тобой, а не зашито в каск.

## Скачать вручную

Последний `.dmg` со страницы [Releases](https://github.com/zdllucky/wotold/releases) — открыть
и перетащить Wotold в Applications. Постоянная ссылка на свежую сборку:
[Wotold-arm64.dmg](https://github.com/zdllucky/wotold/releases/latest/download/Wotold-arm64.dmg).

## Первый запуск: предупреждение Gatekeeper

Сборка **не нотаризована в Apple** — это осознанное решение на время MVP (ограничение R6),
а не признак того, что с файлом что-то не так. macOS откажется открыть приложение с первого
раза.

Как разрешить: **Системные настройки → Конфиденциальность и безопасность** → внизу будет
сообщение о заблокированном Wotold → **«Открыть всё равно»**.

Если предпочитаешь не полагаться на чужую подпись — [собери из исходников](#сборка-из-исходников),
код открыт.

## Что приложение попросит

**Имя и роль.** Нужно, чтобы понимать, кто говорит на твоей дорожке: микрофон — всегда
владелец устройства.

**Микрофон.** Твоя дорожка записи.

**Запись экрана.** Требуется macOS для доступа к системному звуку через ScreenCaptureKit.
Wotold **не сохраняет кадры и не делает скриншотов** — из API берётся только аудио. Проверить
это можно в исходниках, разрешение запрашивается только ради звуковой дорожки.

## Модели

При первом включении Wotold скачивает модели с HuggingFace (одну — из релизов sherpa-onnx на
GitHub). Дальше обработка идёт офлайн; в сеть приложение ходит только за проверкой новой
версии при запуске.

| Пресет | Размер | Чем отличается |
|---|---|---|
| **Light** | ~2 ГБ | Быстрее всех, заметно ниже качество распознавания и саммари |
| **Balanced** | ~4 ГБ | Компромисс по умолчанию |
| **Quality** | ~7 ГБ | Лучшее качество, дольше обработка и сильнее нагрев |

Пресет меняется в **Настройки → Обработка**. Модели при смене пресета автоматически не
удаляются — управление занятым местом вынесено в отдельный экран хранилища, чтобы
приложение не стирало гигабайты за спиной у пользователя (ограничение R12-bis).

Слабое железо не блокирует запуск: проба рекомендует Light и предупреждает, но выбор
остаётся за тобой (R13).

## Первая запись

1. На главном экране — **«Начать запись»** или `⌘⇧R`.
2. **«Стоп»** по окончании. Начнётся локальная обработка.
3. Прогресс виден в карточке звонка: распознавание, диаризация, саммари.

Обработка занимает минуты, а не секунды: STT и LLM считаются на твоём процессоре. Длинная
запись режется на части и обрабатывается по мере готовности.

## Сборка из исходников

```bash
git clone https://github.com/zdllucky/wotold.git
cd wotold
pnpm install
pnpm tauri dev
```

Нужны Node.js ≥ 20.18, pnpm ≥ 10, Rust stable и Tauri CLI 2
(`cargo install tauri-cli --version "^2"`). Подробности — в
[CONTRIBUTING.md](https://github.com/zdllucky/wotold/blob/main/CONTRIBUTING.md).

## Обновления

Приложение проверяет наличие новой версии через полминуты после запуска и дальше раз в шесть
часов. Запрос анонимный: уходит обращение к GitHub за файлом-манифестом, твоих данных в нём
нет.

**Обычное обновление** (minor или patch) показывается тостом с кнопкой «Обновить». Момент
выбираешь ты.

**Обязательное обновление** (мажорная версия либо релиз, помеченный как критический) ставится
само. Но **никогда во время записи или обработки**: оно скачивается, ждёт ближайшего простоя и
устанавливается тогда. Пока ждёт — в шапке висит объяснение. Потерять запись из-за обновления
нельзя.

Проверить вручную: **Настройки → О приложении → Проверить обновления**. Там же видно текущую
версию.

:::caution[Разрешения после обновления]
Сборка подписана ad-hoc, без сертификата Apple. macOS привязывает выданные разрешения к
подписи приложения, а у ad-hoc-подписи нет стабильной идентичности — она меняется с каждой
сборкой. Поэтому после обновления система с высокой вероятностью снова спросит **Микрофон** и
**Запись экрана**, причём второе — через Системные настройки.

Это цена отказа от платного Apple Developer Program, а не дефект. Снимается только настоящей
подписью.
:::

## Прежде чем записывать разговор

В ряде юрисдикций запись разговора требует согласия **всех** участников. Wotold — не бот
в звонке, второй стороне он себя не показывает. Прочитай
[уведомление о записи](../legal/consent/) до первого использования в работе.
