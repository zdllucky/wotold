# Технический долг (аудит 2026-07)

> Groomed-беклог по итогам глубокого аудита [`audits/2026-07-23-deep-review.md`](audits/2026-07-23-deep-review.md) (10 ревью-агентов + adversarial-верификация, 55 подтверждённых находок). Крупнейший фикс аудита — удаление облачного сегмента (`86e17fc`) — уже выполнен; здесь остаток. Все якоря пересверены с кодом 2026-07-24: ни одна из находок не была закрыта cloud-cut'ом.
>
> Файл **самодостаточен**: каждая задача читается без отчёта. Отчёт рядом — за развёрнутым обоснованием, insights и приложениями.

## Как работать с этим файлом

- **Одна задача TD-NN = одна сессия** по PDCA-воркфлоу W3 из [`../CLAUDE.md`](../CLAUDE.md): design gate (если UI) → plan → TDD → verify → `/code-review` → mark done.
- **Якоря дрейфуют.** `file:line` — подсказка, истина — файл + имя символа. Первым делом в сессии сверить якорь.
- **Порядок волн = приоритет по умолчанию.** Внутри волны задачи независимы, если не указано `→ после TD-NN`.
- **Метки:** `(S)` < 2ч · `(M)` полдня-день · `(L)` больше дня · `⚑` release-критично.
- **Закрытие:** `[x]` на месте + строка итога (что за тесты/проверка подтвердили). ID стабильны и не перенумеровываются; новые находки дописываются в конец своей волны.
- **Системные выводы** аудита оформлены не задачами, а правилами → [`../CLAUDE.md`](../CLAUDE.md) §«Инженерные правила». Продуктовые release-блокеры (LICENSE, consent-notice) → [`ROADMAP.md`](ROADMAP.md) §A.

---

## W1 · Конвейер и харнесс

> Сначала чинится станок: пока хуки мертвы, а релизный workflow невалиден, любая последующая сессия работает вслепую.

- [x] **TD-01 ⚑ release-app.yml: дубль `args:` + неподписанный DMG** (S) — в одном `with:` шага «Tauri build + GitHub Release» два ключа `args:` (`.github/workflows/release-app.yml:124` `--features voice-onnx` и `:142` `--target universal-apple-darwin`): YAML last-wins → **прод-DMG собирается без биометрического матчинга** вопреки B3.7, а строгий парсер отвергнет workflow целиком. Рядом `:148-156` ad-hoc `codesign` бежит ПОСЛЕ того как tauri-action уже собрал и залил DMG, и подписывает `.app`, а не публикуемый артефакт — заявленная митигация «damaged, move to trash» не работает. Фикс: один `args` со всеми флагами; подпись перенести внутрь bundling (tauri `signingIdentity: "-"`). Проверка: dry-run на тестовом теге → скачать артефакт → `codesign -dv --deep`, `spctl -a -t open`. Workflow ни разу не исполнялся (git tag пуст) — это единственный непрокатанный путь CI.
  - **Готово:** Слито в один `args`; подпись перенесена в `bundle.macOS.signingIdentity: "-"` + `hardenedRuntime: false`; шаг ad-hoc codesign удалён. Проверено локальной release-сборкой (aarch64): `.app` и все 5 сайдкаров подписаны adhoc ДО сборки dmg, `codesign --verify --deep --strict` проходит, смонтированный `.dmg` содержит подписанный `.app`. **E2E dry-run на теге блокирован #42** (нет minisign-ключей, `pubkey` в tauri.conf.json = PLACEHOLDER) — сборка падает ровно на подписи updater-артефакта.
- [x] **TD-02 ⚑ SHA-pin всех GitHub Actions** (S) — 38 `uses:` в 4 workflow, ноль пиннинга по SHA. Острее всего `tauri-apps/tauri-action@v0` (`release-app.yml:102`, получает `TAURI_SIGNING_PRIVATE_KEY` — компрометация тега = кража ключа подписи автообновлений у всех юзеров) и `anthropics/claude-code-action@beta` (`claude-review.yml:68`, `ANTHROPIC_API_KEY` + `id-token: write`). Фикс: pin на полный commit-SHA с комментарием-версией, Dependabot на `github-actions`. Проверка: `rg -c 'uses:.*@[0-9a-f]{40}' .github/workflows/` = числу `uses:`.
  - **Готово:** Все 38 `uses:` запинены на commit-SHA с трейлинг-версией (13 уникальных actions), добавлен `.github/dependabot.yml` (weekly, prefix `ci`, миноры/патчи одной группой). Двум action'ам, читающим имя ref'а, проставлены явные входы: `toolchain: stable` (×3) и `tool: cargo-llvm-cov`. Проверено: YAML парсится strict-загрузчиком без дублей ключей, пины == uses пофайлово (22/2/3/11).
- [x] **TD-03 Харнесс: мёртвый хук, обходимый гейт, широкий allowlist** (S) — три дыры enforcement'а: (a) `scripts/hooks/post-write.sh:14` читает `CLAUDE_FILE_PATH`, которую Claude Code не выставляет (payload приходит JSON'ом на stdin, как корректно делают три соседних хука) → `cargo fmt`/`cargo check`/`tsc --noEmit` **никогда не запускались**, тихий no-op с мая, при этом CLAUDE.md:163 заявляет механизм активным; (b) `scripts/hooks/pre-write.mjs:23` меряет 800-строчный лимит по `tool_input.content ?? new_string` — для Edit это фрагмент замены, файл можно наращивать бесконечно серией Edit'ов (matcher `Write|Edit` создаёт ложное ощущение покрытия); (c) `.claude/settings.json:35` `Bash(rm:*)` и `:22` `Bash(git checkout:*)` — деструктив без подтверждения при аккуратно гранулированном остальном списке. Фикс: парсить stdin-JSON как в соседях; для Edit оценивать итоговый размер файла с диска; убрать два allow-паттерна. Синхронно поправить описание хуков в CLAUDE.md. Проверка: правка `.rs` → в логе виден реальный `cargo fmt`.
  - **Готово:** `post-write.sh` переписан в `post-write.mjs` (stdin-JSON + `spawnSync({timeout})` вместо отсутствующего на macOS `timeout`; корень репо не хардкодится; contracts проверяется через потребителя). Гейт 800 строк считает итоговый файл. Матчеры → `Write|Edit|MultiEdit`. Из allowlist убраны `rm:*`, `git checkout:*`, `timeout:*`. **Попутно найдено и починено:** whitelist design-gate матчился по абсолютному пути, поэтому правило `.claude/` накрывало путь git-worktree и гейт был полным no-op внутри worktree. Проверено 15 синтетических payload-кейсов.
- [x] **TD-04 Вендорить дизайн-прототип в репо** (S) — источник истины дизайн-системы физически лежит в `~/Downloads/Wotold v2/` (`uikit.css` + `wk-*.jsx`) и `~/Downloads/design_handoff_wotold_assistant/`; на них завязаны CLAUDE.md:70,74,79 и обязательный design gate. Любая другая машина (и любой агент после чистки Downloads) **не может пройти гейт**, дрейф уже виден. Прецедент вендоринга в репо есть: `docs/design/atelier-v2/_reference/`. Фикс: snapshot в `docs/design/wotold-v2/_reference/`, переписать ссылки в CLAUDE.md и в `.claude/skills/design-gate/SKILL.md` (он ещё указывает на `atelier-v2`). Проверка: `rg '~/Downloads' CLAUDE.md docs/design/ .claude/ apps/` пусто (исходная формулировка `CLAUDE.md .claude/skills/` проходила тривиально — в skills этой строки не было вовсе).
  - **Готово:** Завендорено: `docs/design/wotold-v2/_reference/` (13 файлов, замыкание точки входа + `wk-views`/`wk-explore`) и `_reference-assistant/` (весь пакет, 15 файлов). Все внутренние ссылки обеих точек входа резолвятся, файлы байт-идентичны источнику. Переписаны 15 ссылок (CLAUDE.md ×3, README канона, assistant.md, M15 PRD, ROADMAP, 8 provenance-комментариев в исходниках). Удалена конвенция «не вендорится в репо» из assistant.md. README канона актуализирован (shim, слои CSS, статус миграции). **Сверх задачи:** `.claude/skills/design-gate/SKILL.md` и `.claude/commands/design-gate.md` переписаны с мёртвого Atelier-канона (Bordeaux, 3 акцента, Source Serif, `--signal`/`--ink`, старые классы) на Wotold v2.

## W2 · Безопасность и приватность

- [x] **TD-05 Валидация call_id на всех границах доверия** (M) — `apps/desktop/src-tauri/src/call_store.rs:97` `call_dir()` = `calls_root().join(call_id)` без валидации, а `commands/calls.rs:26` `delete_call` и `:64` `read_call_artifact` принимают сырой id из webview: `id=".."` резолвится в `app_data_dir` и `remove_dir_all` сносит **всю БД и все записи**. Тот же класс в MCP: `services/mcp/src/tools.ts:67` `call_id: z.string().min(1)` → path-traversal на чтение (`readArtifact` :51, ограничен именами `recap.md`/`transcript.md`, read-only — MEDIUM). Ирония: `local_engine` имеет `ensure_path_under`, а простейший её потребитель — нет. Фикс: UUID-валидация в `call_dir()` (fail-closed) + `ensure_path_under(calls_root)` defense-in-depth + `z.string().uuid()` в MCP. Тесты: `..`, абсолютный путь, `a/../../b` с обеих сторон.
  - **Готово:** Введён ньютайп `CallId` (новый кросс-платформенный модуль `call_id.rs`): `CallStore` принимает только `&CallId`, поэтому пропустить валидацию в новом callsite нельзя — не скомпилится. `parse()` строже `Uuid::parse_str` (отклоняет urn/braced/uppercase — валидные UUID, но дающие несуществующий путь), `from_db()` для строк из нашей БД. `ensure_path_under` вынесен из `local_engine::llm` в общий модуль и стоит вторым слоем в `remove_call_dir`/`read_artifact`. **Сверх задачи:** закрыты две Tauri-команды, ходившие мимо CallStore ручным `join("calls")` — `local_engine_eval_recap` (чтение любого recap.md в ФС, была реально эксплуатируема) и `get_voice_sample_audio`. MCP: `call_id` под каноническим uuid-regex, фикстуры пересеяны на настоящие UUID. Тесты: 769 rust (+11), 21 mcp (+3), clippy чист.
- [x] **TD-06 Таксономия ошибок сайдкара: rotate ≠ fatal** (M) — `apps/desktop/src-tauri/src/audio/macos.rs:327` dispatcher матчит `"stopped" | "error"` как терминальные, но Swift шлёт `{"event":"error"}` и для **нефатальных** сбоев ротации (`App.swift:147,154`), после которых запись продолжается. Итог: один transient → dispatcher умирает → `stop_recording` достаёт из `terminal_rx` старую ошибку → часовой звонок помечается failed при целых WAV на диске. Фикс: разделить скоуп в NDJSON-протоколе (`rotate_error` non-fatal vs `error` fatal), терминальными считать только fatal + Terminated; recoverable → degraded-флаг (правило 3, инфраструктура в TD-37). Тест: инжект rotate-ошибки → запись жива, звонок ready.
  - **Готово:** Протокол получил нефатальное `rotate_error` (с `leg` и `mic_rotated`); `error` остался фатальным. Найденная сверх задачи асимметрия: при провале системной ноги мик уже ротирован, поэтому шлём И `rotate_error`, И `rotated` с `system_bytes=0`+warning — иначе индекс не продвинется и дорожки разъедутся. Плюс recovery: рекордеры обнуляют `wavWriter` между close и open (раньше там оставался закрытый writer и дорожка была мертва до конца записи). Rust: `classify_event` вынесена чистой, ветка `NonFatal` не убивает диспатчер. Пакет сайдкара разрезан на `WotoldAudioCore` + executable + testTarget. **Проверено на живом бинаре:** форсированный сбой системной ротации → сессия жива, следующая ротация подняла дорожку заново. 25 swift-тестов (первые вообще) + 773 rust (+4). Живой прогон полного пути через приложение — не делался.
- [x] **TD-07 Пауза реально останавливает захват** (M) → после TD-06 — `apps/desktop/src-tauri/src/commands/recording.rs:567-589` пауза живёт только в БД (`db::pause_call`), сайдкар о ней не знает: mic и system продолжают писаться, транскрибируются и попадают в саммари. Комментарий `TODO(W2 v2)` оправдывается «тишина отрежется», но юзер жмёт паузу именно чтобы сказать приватное — это речь, а не тишина. Прямое нарушение ожидания согласия (C1-смежное), **не покрыто R1–R13**. Плюс `duration` вычитает паузы, а аудио их содержит — таймстемпы едут. Фикс: `{"cmd":"pause"}`/`{"cmd":"resume"}` в протокол (расширяет TD-06), Swift дропает буферы или суспендит tap. Тест: длительность WAV < wall-clock на длину паузы. Минимальная альтернатива при откладывании — честный warning в UI.
  - **Готово:** Протокол расширен `pause`/`resume` (поверх TD-06); оба рекордера дропают кадры в обработчике до записи в WAV, `latestRms` на паузе обнуляется. Команда уходит сайдкару **перед** записью в БД — иначе при сбое UI показал бы «на паузе» при пишущем микрофоне. Побочно снято рассогласование из `[M13 fix]`: `chunk_start_ms` был pause-inclusive, `total_ms` — pause-subtracted; теперь шкалы сходятся. **Измерено на живом бинаре:** wall-clock ~7с (запись 2с + пауза 3с + запись 2с) → 3.54с в mic.wav и 3.62с в sys.wav, то есть пауза физически отсутствует. 30 swift (+5) и 773 rust тестов, регрессия учёта пауз в оркестраторе зелёная. Живой прогон через приложение не делался.
- [x] **TD-08 llm_server: эфемерный порт + api-key** (S) — `apps/desktop/src-tauri/src/local_engine/llm_server.rs:27` `SERVER_PORT = 47331` фиксирован, аргументы `:117-136` без `--api-key`: любой процесс на машине дёргает `/completion` (бесплатный inference на чужих 2-5GB + DoS очереди рекапов при `--parallel 1`), а процесс, занявший порт раньше и отвечающий на `/health`, **получит все промпты с транскриптами**. CLI-путь получил все W5-митигации (0600, O_EXCL, path checks) — server-путь при переезде фичи их не унаследовал. Фикс: случайный свободный порт + сгенерированный api-key в args (валидатор в capabilities) и в заголовок клиента.
  - **Готово:** Порт эфемерный (`bind 127.0.0.1:0`), ключ — 32 hex на сессию. **Ключ уехал в env `LLAMA_API_KEY`, а не в `--api-key`** как предлагала задача: аргументы видны в `ps aux` любому пользователю, то есть секрет был бы доступен ровно тому, от кого защищаемся (проверено — в argv сервера его нет). Побочно: `capabilities/default.json` менять не пришлось. Случайного порта мало — `/health` у llama.cpp публичен, поэтому добавлена аутентифицированная проверка `/props`: чужой процесс на порту не выдаст себя за наш сервер. Плюс `Drop` для `LlamaServer` (раньше дропнутый handle оставлял процесс жить). **Проверено на живом бинаре с прод-аргументами:** `/completion` без ключа → 401, с ключом → 200 с реальной генерацией; `/props` с чужим ключом → 401. 777 rust-тестов (+4). Путь через приложение на живом звонке не проверялся.
- [x] **TD-09 MCP: эскейп LIKE-wildcards** (S) — `services/mcp/src/db.ts:121-128` `findContactsByName` строит `%${query}%` без экранирования `%`/`_` и без `ESCAPE`, тогда как соседний `searchCalls` (`:83-94`) использует `escapeLikePattern` + `ESCAPE '\\'`. Непоследовательность: `contact_name='%'` = match all. Read-only, LOW — но паттерн уже есть в файле, стоимость фикса нулевая. Тест: имя с `%` и `_`.
  - **Готово:** Обёрнуто в существующий `escapeLikePattern` + добавлена `ESCAPE` на оба `LIKE` (без клаузы эскейп не значил бы ничего). Тест: контакт с литеральными `%`/`_` в имени — запрос `%` возвращает только его, а не весь список.
- [x] **TD-10 models.rs: целостность скачивания** (M) — три связанных дыры в `apps/desktop/src-tauri/src/local_engine/models.rs`: (a) `:151` QWEN25_0_5B и `:201` SILERO_VAD с `sha256: "PLACEHOLDER_REFRESH_VIA_SCRIPT_BEFORE_PRODUCTION"` — модели качаются целиком (до 380MB) и гарантированно падают `VerifyFailed`, при этом видны в UI каталога, а `check_status_fast` для них деградирует до «len>0 → Present» (любой мусорный `silero-vad-v5.bin` уйдёт в whisper-cli); (b) `:478` фиксированное имя партиала `{id}.bin.partial` + hasher по **сетевому потоку**, а не по файлу → два параллельных download одного id (двойной клик, авто-догрузка эмбеддера) дают «верифицированный» rename битого файла; (c) `commands/local_engine.rs:63,101` комментарии обещают lazy SHA-verify перед использованием модели — её нет, рантайм проверяет только размер. Фикс: прогнать `scripts/refresh-model-catalog.sh` и вписать реальные SHA/size; per-id async-мьютекс + уникальное имя партиала + контрольный хеш по записанному файлу до rename; комментарии привести к реальности («SHA только при download, рантайм — size-only») либо реализовать одноразовую фоновую проверку.
  - **Готово:** Вписаны реальные SHA/size с HF, ветка деградации placeholder удалена (инвариант «нет placeholder» — тестом). **Сверх задачи:** размеры обеих записей тоже были неверны (silero 1_627_136 → реально 885_098), из-за чего корректно скачанная модель помечалась бы Corrupted. Гонка скачиваний закрыта: per-id async-мьютекс (OnceLock+tokio, не LazyLock — MSRV 1.77) взят до идемпотентной проверки, уникальное имя партиала, SHA считается по записанному файлу (stream-hasher убран). Скрипт: дописаны обе записи в MODELS + починен kind-case (silero → Stt, не Llm). Лживые комментарии в commands/local_engine.rs приведены к реальности. **Проверено живым скачиванием silero (885KB) настоящим download():** SHA сошёлся, статус Present, партиалов не осталось. 779 rust-тестов, clippy чист.
- [x] **TD-11 Temp-гигиена local_engine** (S) — `local_engine/llm.rs:464-469` (prompt/grammar/schema) и `stt.rs:328` (whisper-JSON) чистятся обычным кодом после `await`: при `JoinHandle::abort()` (отмена звонка, выход) `SidecarGuard` убьёт процесс, но `remove_file` не выполнится — файлы **с полным транскриптом** остаются в tmp. Плюс `stt.rs:568-575`: JSON создаёт whisper-cli с дефолтным umask (0644), `chmod 0600` накладывается только после Terminated — весь период транскрипции контент читаем. Фикс: RAII-guard на удаление (по образцу `SidecarGuard`) + отдавать `-of` в приватную поддиректорию, созданную заранее с `0o700`; janitor `wotold-*` в tmp на старте. Тест: perms до записи, отсутствие файлов после abort.

## W3 · Корректность: запись и пайплайн

  - **Готово:** Введён `TempFileGuard` (RAII рядом с `SidecarGuard`): файлы и директории чистятся через **синхронный** `std::fs` в `Drop` — работает при abort/panic/раннем `?`, где `tokio::fs::remove_file` после await не срабатывал. **Сверх задачи:** утечка была и без отмены — между созданием файлов и cleanup стоят ранние `?` в llm.rs. Whisper-JSON: output теперь в приватную поддиректорию `0o700`, созданную до спавна, — umask 0644 самого файла неважен, войти в директорию чужой UID не может. **Проверено живым whisper-cli:** `out.json` пишется 0644 внутри `drwx------`, `remove_dir_all` уносит транскрипт рекурсивно. 5 юнитов на guard (включая регрессию abort-утечки), 784 rust-теста, clippy чист.
- [x] **TD-12 `pipeline_finished` эмитится всегда** (S) — `apps/desktop/src-tauri/src/pipeline/mod.rs:312` `db::mark_call_ready(...).await?` в success-ветке: если UPDATE упал (busy pool, disk full), `?` выходит из функции ДО `bus.pipeline_finished(&event)` (`:341`) и без `fail_recording_with_reason`. Пайплайн фактически успешен (транскрипт и рекап на диске), но статус навсегда `processing`, фронт не получает ни `finished`, ни `failed`. Err-ветка того же match делает и persist, и event — асимметрия только на success-пути. Фикс: match вместо `?`, событие в обеих ветках. Тест: fail `mark_call_ready` → событие пришло.
  - **Готово:** Ошибка `mark_call_ready` больше не короткозамыкает через `?`: решение вынесено в чистую `finish_event` (pipeline-fail → failed; иначе mark-ready-fail → failed; иначе ready), событие `pipeline_finished` эмитится безусловно, статус персистится один раз, `spawn_index` только на настоящем ready. Возврат run(): ready→Ok, иначе→Err(reason) — контракт потребителей сохранён. 4 юнита включая регрессию «успех-но-статус-не-записался». 788 rust-тестов, clippy чист.
- [x] **TD-13 pipeline_runner: гонка insert-after-spawn** (M) — `apps/desktop/src-tauri/src/services/pipeline_runner.rs:300` task спавнится, `:326` сам удаляет себя из map, `:328` регистрация происходит **после** спавна. Быстро упавший task (missing preset) выигрывает гонку: его `remove` — no-op, затем `insert` кладёт handle завершённой задачи, который никто не уберёт. Для `spawn_regen` (spawn `:151`, insert `:215`) последствие критичнее: guard `contains_key` навсегда возвращает «call_already_processing» до рестарта приложения. Фикс: занимать слот под lock ДО spawn (placeholder → обновить handle), либо проверять `handle.is_finished()` в guard и вычищать stale. Тест: мгновенно падающий task → слот свободен, regen доступен.
  - **Готово:** Гонка убрана **барьером регистрации**: `insert` до `spawn` невозможен (handle появляется только из `spawn`), поэтому тело задачи ждёт oneshot-сигнал «ты зарегистрирован» и лишь затем работает — к моменту самоудаления запись гарантированно в реестре. Протокол вынесен в `spawn_registered` и живёт в одном месте (дублирование в двух точках и было причиной, что баг завёлся дважды); тип реестра и все пять читателей не тронуты. **Регрессионный тест проверен на подлинность:** с отключённым барьером падает, с барьером проходит. 791 rust-тест (+3), clippy чист.
- [x] **TD-14 chunk_orchestrator: гонка stop vs rotated** (M) — `apps/desktop/src-tauri/src/pipeline/chunk_orchestrator.rs:165` ветки `select!` опрашиваются в случайном порядке: если Stop пришёл через секунды после ротации, `rotated` может лежать непрочитанным → `chunk_idx` не инкрементирован, `final_chunk_idx` (`:318`) указывает на закрытый chunk, а уже открытый сайдкаром chunk k+1 не получает ни строки в БД, ни STT. Аудио не теряется (merger сканирует диск), но **последние секунды разговора отсутствуют в транскрипте без единого warn**. Фикс: после break неблокирующий `try_recv`-drain `rotate_rx` и enqueue полученных чанков до подсчёта `final_chunk_idx`. Тест: stop сразу после rotate → хвост в транскрипте.
  - **Готово:** Неблокирующий `try_recv`-drain после выхода из loop'а, ДО подсчёта `final_chunk_*`. **Сверх задачи выяснилось:** непрочитанное событие теряло не только инкремент индекса, но и **enqueue закрытого чанка** — он делается только внутри rotated-ветки. Обработка вынесена в `apply_rotated`, переиспользуется веткой `select!` и drain'ом (дублирование = «одинаковый контракт, разная зрелость», правило 2). Drain покрывает все три `break`. Добавлено `summary.rotated_drained_on_stop` — гонка наблюдаема. Тесты без sleep: гонка строится детерминированно (обе ветки готовы до первого polling'а), инвариант проверяется 20 прогонами. **Подлинность регрессии проверена:** без drain'а падает (`left: 0, right: 1`), с drain'ом проходит. 793 rust-теста (+2), clippy чист.
- [x] **TD-15 Проглоченные ошибки и паника в error-path** (S) — три мелочи одним заходом: (a) `pipeline/mod.rs:1069` re-STT при пине языка — `if let Ok(re) = ...` вообще без Err-ветки (обе дорожки), молчаливый возврат к mis-detected языку, ради борьбы с которым фича и писалась; (b) `db/mod.rs:66-72` карантин повреждённой БД переименовывает только `app.db`, оставляя `-wal`/`-shm` (старый WAL вольётся в свежую базу), а ошибка `rename` глотается через `.ok()` → код молча открывает заведомо битый файл; (c) `local_engine/llm.rs:637` `&s[..s.len().min(512)]` — байтовый срез из `from_utf8_lossy` паникует на границе многобайтового символа (кириллица в путях GGUF, вставленный самим lossy U+FFFD) ровно в аварийных ветках. Фикс: `match` с warn; переименовывать/удалять три файла и логировать ошибку; `floor_char_boundary`-семантика + тест с кириллическим stderr >512 байт.
  - **Готово:** (a) re-STT: `if let Ok` → `match` с warn на обеих дорожках. (b) карантин БД: извлечён `quarantine_corrupt_db`, переносит **все три** файла (app.db + `-wal` + `-shm`), ошибка логируется через `log::error` вместо `.ok()`; удалять при неудаче намеренно не стали — карантин ради сохранности данных. (c) извлечён `truncate_at_char_boundary` с ручным откатом по `is_char_boundary` (`str::floor_char_boundary` **нестабилен даже на rustc 1.95** — проверено компиляцией). **Подлинность регрессий проверена откатом:** (b) падает «-wal не должен остаться», (c) даёт ровно ту панику из аудита. Первая версия (c)-теста зеленела на сломанном коде — в строке из одной кириллицы все границы чётные, а 512 чётное; добавлен ведущий ASCII-байт. **(a) юнитом не покрыта честно** — ветка внутри `run_local_inner`, ценность диагностическая. 800 rust-тестов (+7), clippy чист.
- [x] **TD-16 speakers.rs: подтверждённое пользователем неприкосновенно** (M) — `apps/desktop/src-tauri/src/db/calls/speakers.rs:18` `insert_speaker_suggestions` делает `DELETE ... WHERE call_id=?1 AND speaker_tag != 'owner'` — **сносит и строки с `confirmed=1`**, тогда как соседний `prune_call_speakers_not_in` (`:163`) бережно их сохраняет. Мина под R2: как только `identify_speakers` подключат к пайплайну, reprocess молча сотрёт все ручные привязки. Рядом `:226/237` `confirm_call_speaker` читает contact/speaker-метаданные ДО `pool.begin()` (`:245`) — конкурентный pipeline может перезаписать cluster между чтением и INSERT (в биометрию контакта уйдёт устаревший embedding), а UPDATE `:247` не чистит `auto_bound_at` (ручная привязка выглядит автоматической, UI показывает «↩ отменить»). Фикс: `AND confirmed = 0` в DELETE, оба SELECT внутрь транзакции, `auto_bound_at = NULL`. Тесты на оба инварианта.
  - **Готово:** Добавлен `AND confirmed = 0` в DELETE (та же клауза, что в соседнем `prune_call_speakers_not_in` — twin parity); оба SELECT'а перенесены внутрь транзакции (конкурентный `set_call_speaker_cluster` мог отправить в биометрию устаревший embedding); `auto_bound_at = NULL` при ручном confirm. **Важно:** функция сегодня НЕ подключена к пайплайну (`identify_speakers` не зовётся ниоткуда — проверено `rg`), это предохранитель на момент wire-up #26, а не активный баг. UPSERT не брали: `UNIQUE(call_id, speaker_tag)` в схеме нет. Подлинность обеих регрессий проверена откатом. 21 тест speakers (+2).
- [x] **TD-17 lifecycle: FSM-гейты для звонков + LIMIT** (M) — `apps/desktop/src-tauri/src/db/calls/lifecycle.rs:233` `finish_recording` и `:601` `fail_recording_with_reason` пишут по `WHERE id = ?1` без проверки текущего статуса и без контроля `rows_affected`: failed/ready звонок можно вернуть в processing поздним stop-flow. Для `call_chunks` та же команда защищена гейтом `WHERE status='pending'/'processing'` (`db/chunks.rs:104-125`) — twin parity нарушена (правило 2), причина историческая: chunks писались позже с учётом опыта гонок. Там же `:634` `list_calls` — `SELECT` 23 колонок без LIMIT, тянется целиком на каждый рендер списка и на каждое событие пайплайна. Фикс: статусные guard-ы + warn на нелегальный переход по образцу `mark_chunk_*`; keyset-пагинация по индексированному `started_at`.
  - **Готово:** Гейты по образцу `mark_chunk_*`: `finish_recording` — `status = 'recording'`, 0 строк → `Err` (функция обязана вернуть Call); `fail_recording_with_reason` — `status IN ('recording','processing')`, 0 строк → **warn + Ok, не Err** (живёт в error-path; новая ошибка поверх исходной потеряла бы первичную причину — так же требует и формулировка аудита). Побочно перестал перетираться `ended_at` у готовых звонков. **Закрыт обход:** cancel-restore в `pipeline_runner` писал `status='ready'` сырым SQL — побайтовой копией `mark_call_ready`, мимо гейтов; переведён на db-слой. Сырой SQL в reprocess оставлен — переход `ready|failed → processing` легален по замыслу. **Пагинация `list_calls` вынесена в TD-42** (27 callsite'ов). Подлинность проверена откатом гейтов; все 802 существующих теста зелёные.
- [x] **TD-18 CPU-heavy код уходит с async-executor** (M) — два места блокируют tokio worker, на котором крутятся и Tauri-команды UI: (a) `pipeline/summary_validator.rs:135` `substring_fuzzy_score` — sliding-window Левенштейн O(len·n²), причём комментарий `:182` утверждает «m, n ≤ 200 (quote length cap)», а cap **нигде не enforced** (цитата приходит от LLM без усечения); fast-path `contains()` спасает только verbatim-цитаты, а весь смысл валидатора — фабрикованные, для которых всегда медленный путь: часовой звонок ≈ 2.4 млрд операций на цитату; (b) `extract_clusters` (WAV I/O + ONNX-инференс на каждый сегмент) вызывается синхронно из async в `pipeline/chunk_runner.rs:334` и `pipeline/mod.rs:1426,1766` — сотни инференсов на длинном звонке. Непоследовательно с соседями: `SortformerDiarizer` и `audio_merger` корректно обёрнуты в `spawn_blocking`. Фикс: реальный cap цитаты + `spawn_blocking` для обоих (правило 5). Проверка: бенч на длинной фабрикованной цитате, отзывчивость UI во время рекапа.
  - **Готово:** (a) Cap цитаты стал настоящим: `MAX_QUOTE_CHARS = 200`, усечение по границе **символа** (`floor_char_boundary` до сих пор nightly — тот же ручной обход, что в TD-15), и стоит **после** fast-path `contains`, чтобы честная длинная цитата по-прежнему давала 1.0. Комментарий у `levenshtein` перестал врать. (b) `spawn_blocking` для `strip_unverified_evidence` + `dedup_items` и для `extract_clusters` — актуальные async-контексты оказались другими, чем в тексте задачи (файл сдвинулся): `relabel_owner_on_mic_full_file`, `run_cluster_pipeline` и `run_chunk` (через синхронный `build_chunk_embeddings_json`). Обёртки положены рядом с алгоритмом (`summary_validator::strip_and_dedup`, `clusters::load_and_extract_clusters`), а не на call-site — перенос в пул есть свойство алгоритма. **Сверх задачи:** в `load_and_extract_clusters` переехала загрузка эмбеддера — оба вызывающих дублировали её байт-в-байт, и она сама блокирующая (чтение модели + инициализация ONNX-сессии). `golden_eval` не тронут: `run_golden_case` синхронная, это eval-харнесс. **Подлинность проверена откатом:** без cap score 0.15, с ним 0.93. Fuzzy-примитивы вынесены в `summary_validator/text_similarity.rs` — исходный файл (787 строк) не давал добавить cap; подмодуль, а не сосед по `pipeline/`, потому что в `pipeline/mod.rs` (2461 строка) нельзя добавить даже строку `pub mod`. **Осознанное отступление:** +2 строки в `chunk_runner.rs` (851) применены мимо гейта вручную, декомпозиция — TD-41. 817 rust-тестов (+10), clippy чист.
- [x] **TD-19 indexer: батч без молчаливых потерь** (S) — `apps/desktop/src-tauri/src/assistant/indexer.rs:406` `rows.iter().zip(vecs.iter())` не проверяет, что эмбеддер вернул ровно `len(rows)` векторов: при частичном ответе лишние строки молча выпадают. В связке с циклом `embed_backfill_with` (`:443-456`) «пока `list_passages_missing_embedding` непуст» это даёт **бесконечный цикл без прогресса и без лога**, если эмбеддер стабильно возвращает Ok с недобором. Фикс: `vecs.len() == rows.len()` иначе Err; guard на прогресс в цикле (`blobs.is_empty() → break` с warn).
  - **Готово:** Проверка `vecs.len() == rows.len()` → `Err` (раньше `zip` усекал молча) + guard нулевого прогресса в backfill-цикле, который иначе крутился вечно без логов. Подлинность проверена откатом: тест краснеет, в выводе видно усечение 3→2. 807 rust-тестов (+2).
- [x] **TD-20 stt: галлюцинации по контексту, а не по точному совпадению** (S) — `apps/desktop/src-tauri/src/local_engine/stt.rs:352-356` `HALLUCINATIONS_EXACT` содержит `"you"`, `"thank you"`, `"thanks"`, `"bye"`, `"goodbye"`, и `is_hallucination` (`:410`, exact-match `:423`) безусловно их дропает — реальные короткие реплики вежливых концовок систематически теряются из транскрипта. Фикс: учитывать контекст (позиция у края записи, длительность сегмента, доля сегмента в чанке) вместо чёрного списка; таблица кейсов в тесте.
  - **Готово, но не так, как задумывала задача.** Сначала замерил на настоящем сайдкаре ([`docs/audits/2026-07-27-td20-whisper-confidence.md`](audits/2026-07-27-td20-whisper-confidence.md)), и замеры поменяли решение трижды. (1) **Масштаб бага меньше заявленного:** фильтр — exact-match по голой строке, а whisper почти всегда пунктуирует реальную речь, поэтому `'Thanks.'` не совпадал с `thanks` и выживал случайно. Реально терялась только бесточечная форма — воспроизведено на произнесённом вслух «You», которое whisper отдал как `' you'`. (2) **Предложенные признаки не годятся:** на 3 секундах тишины whisper выдаёт сегмент 0–2060 мс, то есть растягивает стамп на саму тишину — ни длительность, ни разрывы не разделяют случаи; вдобавок `build_transcript` работает на chunk-локальных офсетах и позиции в звонке не знает вовсе. (3) **Разделяет вероятность, и она уже была в JSON:** `--output-json-full` отдаёт `tokens[].p`, а десериализация их выбрасывала. Одна и та же строка `' you'` — 0.135 на тишине против 0.520 произнесённой вслух.
  - **Фикс:** фильтр разделён на артефакты (безусловный дроп, как раньше) и спорные формулы (дроп только при `confidence < 0.30`). Порог — геометрическая середина между худшей галлюцинацией (0.191) и слабейшей реальной речью (0.513), сдвинутая в сторону сохранения речи. Вероятность **не** заменяет остальной фильтр: русские субтитр-credits с тишины идут с avg_p 0.805–0.934, шумовые loop'ы — 0.93, поэтому substring-список и repetition-машинерия нетронуты. Сигнал везёт `TranscriptSegment.confidence` — поле уже было в контракте и всегда стояло `None`, так что `packages/contracts` не менялся. Служебные токены исключены из среднего, и это несущее решение: наивное среднее даёт 0.499 у галлюцинации против 0.553 у речи, разделение исчезает (тест есть). Подлинность проверена откатом — падают 5 тестов, включая парный на реальных фикстурах сайдкара. 831 rust-тест (+12).
  - **Границы, честно:** выборка синтезирована `say` (n=13 речи / n=9 галлюцинаций, две модели, одна машина) — реальный телефонный тракт даст ниже, что сдвигает порог вниз, а не вверх. Русский филлер whisper'а по-прежнему не фильтруется, список только английский: симметризовать без замеров = завести новый класс ложных удалений. **Старые звонки не лечатся** — слова были удалены на этапе STT, в `call_chunks.transcript_json` их уже нет, пересборка не поможет (в отличие от `[FOREIGN]`-фиксов, которые `merge.rs` умеет чинить на переассамблее). `real_word_count` теперь считает уверенные «thanks» за реальные слова, то есть язык чуть охотнее пинится на вежливом чанке; тест на этот call-site не добавлен — `chunk_runner.rs` 852 строки, гейт когезии.
  - **Побочно:** `stt.rs` 1048 → 555. Фикс требовал править и фильтр, и парсинг, а файл не мог расти; выделены `local_engine/hallucination.rs` и `local_engine/whisper_json.rs` двумя чисто механическими коммитами до самого фикса.
- [x] **TD-21 Swift sidecar: twin-parity pass** (L) — довести `ProcessTapRecorder` до зрелости `AudioRecorder` (правило 2) и закрыть общие дыры в `apps/desktop/sidecars/macos-audio/Sources/WotoldAudio/`: (a) `ProcessTapRecorder.swift:212` `stop()` закрывает WAV и обнуляет поля на вызывающем потоке, тогда как `handleAudio` исполняется на `self.queue` — write-after-close на недописанном хвосте финального system-чанка; ровно эту гонку в близнеце уже чинили (`AudioRecorder.swift:176-184`, `[M13 fix]`, close внутри `queue.sync`); (b) `:49` повторный `start()` не проверяет занятые `tapID`/`aggregateID` и перезаписывает поля — старые CA-объекты живут до выхода процесса (в `AudioRecorder.start` есть `try? stop()`); (c) `WAVWriter.swift:76` `dataBytes &+=` на `UInt32` заворачивается после 4 GiB — заголовок молча становится мусором (нужен cap + warning, WAV всё равно ограничен 4 GiB); (d) `AudioRecorder.swift:23` `latestRms` пишется из audio-callback и читается таймером без синхронизации — UB по модели памяти, первый кандидат на падение под Swift 6 strict concurrency; (e) ни один рекордер не подписан на `AVAudioEngineConfigurationChange` — отвалившаяся Bluetooth-гарнитура или выдернутый USB-микрофон **молча убивают mic-дорожку**, UI показывает идущую запись. Фикс (e): перезапуск engine с новым форматом либо `{"event":"device_lost"}` в протокол (расширяет TD-06).
  - **Готово (a)–(d).** (a) `stop()` переведён на `queue.sync` по образцу близнеца — раньше close+обнуление шли на вызывающем потоке параллельно с уже поставленным блоком IOProc; разрушение Core Audio объектов вынесено ПОСЛЕ sync. (b) повторный `start()` теперь останавливает предыдущий. (c) счётчик WAV стал 64-битным, запись сверх потолка RIFF отклоняется явной ошибкой с однократным сообщением, `write(buffer:)` возвращает 0 — счётчики звонка не врут. (d) введён `AtomicLevel` на `OSAllocatedUnfairLock` (macOS 13+; `Synchronization.Mutex` потребовал бы macOS 15), подключён в оба рекордера; побочно отбрасывается NaN, который иначе залипал бы в индикаторе навсегда. Тесты: `WAVWriter` вообще не был покрыт — добавлены `WAVWriterTests` и `AtomicLevelTests`, 30 → 40 Swift-тестов. Подлинность (c) проверена откатом (4 падения).
  - **Готово (e), но не так, как формулировала задача.** Сначала замерил живьём — [`docs/audits/2026-07-27-td21e-device-loss.md`](audits/2026-07-27-td21e-device-loss.md). (1) Утверждение аудита подтвердилось: смена устройства молча останавливает кадры, дорожка не оживает сама. (2) **Ни уведомление, ни `isRunning` не годятся как признак живости:** уведомление приходит с задержкой 0.5–3.5 с, пачками по три, и один раз ещё до всякой смены; `isRunning` всё это время возвращает `true` при нуле кадров. Поэтому решение принимает watchdog по факту прекращения кадров. (3) **Восстановление работает ровно одним способом из трёх:** переустановка tap'а кидает ObjC-исключение и роняет процесс, `start()` без переустановки даёт `-10868`, работает только новый `AVAudioEngine` целиком; частота железа при этом реально меняется (24000 → 48000 Гц), так что конвертер обязателен к пересозданию. Сквозная проверка: провал 4.8 с обнаружен, сообщён, дорожка восстановлена — 16.8 с аудио из 22 с записи против ~5 с до фикса. 47 Swift-тестов, 832 rust-теста.
  - **Не сделано, осознанно:** (i) системная дорожка (Core Audio Process Tap) не покрыта — у неё нет `AVAudioEngine`, нужен слушатель свойств Core Audio; заведено как **TD-44**. (ii) Пользователь узнаёт о провале только из лога: постоянного degraded-флага нет, это **TD-37**, а фронтенд сегодня не слушает ни одного audio-события кроме `audio:level`. (iii) Тишина в дыру не дописывается — дорожки сливаются по таймкодам, так что провал сдвигает mic-реплики относительно system, но дописывать в запись звонка выдуманное молчание — фабрикация содержимого; решение за владельцем, заведено как **TD-45**.

## W4 · Ассистент

- [x] **TD-22 Роутер и retrieval: меньше ложных перехватов** (M) — `apps/desktop/src-tauri/src/assistant/router.rs:158` ListCalls срабатывает на `has_any(["какие","покажи","список","перечисли"]) && has_any(CALL_WORDS)`: «Какие решения приняли на встрече?» → юзер получает **список звонков**, LLM и retrieval не вызываются вообще. Аналогично Stats (`:125`): «Сколько задач раздали на встрече?». Direct-ответ безальтернативен (нет fallthrough при низкой уверенности), поэтому false positive = гарантированно неверный ответ; негативный тест-лист (`:855`) содержит только «что…»-формулировки и дыру не ловит. Там же `:130` LastCall покрывает только мужской род — «когда была последняя встреча?» уходит мимо роутера. И `retrieval.rs:320` (плюс `:198`): темпоральный период фильтруется **после** SQL `LIMIT` — вопрос «что обсуждали вчера про бюджет» на большом архиве даёт ложное «ничего не найдено», хотя вчерашний звонок содержит ответ (cosine-канал фильтрует правильно, BM25 — нет). Фикс: guard «остались ли значимые контентные токены» для ListCalls/Stats по образцу `when_topic`; женские формы; period-set в SQL-условие до LIMIT. Тесты: негативы «какие решения приняли на встрече», «сколько задач раздали», «когда была последняя встреча».
  - **Готово.** Guard `has_topic_beyond` по образцу `when_topic`: если после вычёркивания триггеров, слов о звонке, периода и служебных слов что-то осталось — вопрос про содержание, а не про список. Список служебных слов допустим сознательно: предлоги, местоимения и связки — замкнутый класс, в отличие от содержательных слов; это не тот случай, когда чёрный список догоняет открытое множество. Женские формы для LastCall добавлены. `PERIOD_WORDS` дублирует вокабуляр `period_range`, и тест не даёт спискам разойтись — проверяет **не** «каждое слово парсится в одиночку» (год парсер намеренно требует с квалификатором, это всплыло при написании теста), а то, что важно: фраза с периодом не выглядит темой И по-прежнему перехватывается.
  - **Третья часть (период после LIMIT) тоже закрыта:** ограничение по звонкам уехало в сам SQL-запрос (IN-список с плейсхолдерами по образцу `prune_call_speakers_not_in`), пустой набор короткозамыкается — `IN ()` в SQLite синтаксическая ошибка. Регрессия воспроизведена буквально: 5 «шумных» звонков с той же темой, лимит 3, искомый шестой; без условия тест краснеет с `["noise0","noise1","noise2"]`.
  - **Побочно:** `router.rs` 876 → 610 (выделены `assistant/period.rs` и `assistant/direct.rs`), `db/assistant.rs` 860 → 806 (выделен `db/assistant_search.rs` по образцу соседнего `assistant_embeddings.rs`). Все три файла не могли расти. 843 rust-теста.
- [x] **TD-23 ask(): детерминизм без LLM + нейтрализация заголовков фрагментов** (S) — `apps/desktop/src-tauri/src/assistant/mod.rs:368` прод-обёртка безусловно строит `build_local_llm_provider` ДО `ask_core_with` (`:386`), который только потом зовёт роутер: при невыбранном пресете мета-вопросы, refusal и empty-ветки — **весь смысл роутера M16.4 «нулевая латентность без LLM»** — падают ошибкой «модель не установлена». Фикс: ленивый провайдер (Option/Lazy), поднимать в момент входа в LLM-путь. Рядом `answer.rs:141-145` `neutralize_markers` закрывает только `<<<`/`>>>`, но не внутренний формат заголовка фрагмента (`build_input:186` `[{n}] «{title}»`, `:199` `\n---\n`): участник звонка может надиктовать текст, который модель воспримет как отдельный фрагмент, а `used_fragments` после клэмпа сошлётся на реальный — искажение атрибуции. Фикс: экранировать `^---$` и `^\[\d+\] «` внутри текста пассажей. Тест: подделанный заголовок в пассаже.
  - **Готово.** (a) `LazyLocalProvider`: провайдер строится при первом обращении к `generate`, а не до роутера. Ошибка построения намеренно **не** кэшируется — «пресет не выбран» устраняется из настроек, и следующий вопрос должен работать без перезапуска. Тест проверяет свойство, а не реализацию: провайдер, взрывающийся при любом обращении, и мета-вопрос, который обязан ответиться; вторая половина — что контентный вопрос этой веткой НЕ отвечается, иначе первый зеленел бы и на сломанном роутере. (b) Экранируются `^---$` и `^[N] «` внутри пассажа: подделанный заголовок с чужим номером заставлял `used_fragments` после клэмпа сослаться на настоящий фрагмент, то есть атрибуция уезжала на источник, которого модель не читала. Экранируется только начало строки — обычная речь не портится, на это есть отдельный тест. Подлинность обеих регрессий проверена откатом. 845 rust-тестов.

## W5 · Фронтенд

- [x] **TD-24 CallDetailPage: ошибка действия не роняет страницу + отмена загрузки** (M) — `apps/desktop/src/pages/CallDetailPage.tsx:384` ранний `if (error) return <p role="alert">` использует общий error-state, в который пишут обработчики **несмертельных** действий (reprocess `:234`, unbind/export `:288`, regenerate `:306,:330`, delete `:378`, retry `:491,:564`): упавший экспорт markdown заменяет весь открытый звонок — транскрипт, плеер, рекап — одним красным абзацем без retry и без «назад». Рядом `hooks/useCallDetail.ts:149-213` `Promise.allSettled` из 12 ресурсов не имеет cancelled-флага, а страница рендерится без `key={callId}` → переход на другой звонок не размонтирует компонент, старый resolve перезаписывает данные нового (в соседнем `useCallAudio.ts:62` флаг есть). Фикс: разделить fatal load-error и action-error (баннер/тост поверх контента); cancelled-флаг или эпоха в хуке. RTL: смена звонка mid-flight, фейл действия → страница жива.
  - **Готово.** Ошибки несмертельных действий уходят в существующий тостер (`.toast--danger`), error-state остался фатальной ошибкой загрузки — новой разметки не понадобилось, design gate прошёл на готовом компоненте. В хук добавлен cancelled-флаг по образцу соседнего `useCallAudio`, на монтировании — `key={detailCallId}` как вторая линия.
  - **Регрессионный тест был фиктивным дважды.** Первая версия делала `unmount` вместо `rerender`, то есть имитировала уже починенное поведение; вторая матчила аргумент `invoke` как `callId`, тогда как API передаёт `id` — ветка старого звонка не срабатывала вовсе. Обе были зелёными на сломанном коде. Итоговая падает на откате с «Found multiple elements with the text: Старый звонок».
  - **Не покрыт тестом** путь action-error: чтобы его дёрнуть, нужен клик по пункту меню за дропдауном. Замена приёмника механическая, проверена tsc и тем, что `setError` в файле не осталось вовсе. 592 фронтенд-теста.
- [x] **TD-25 i18n нижних слоёв** (M) — `apps/desktop/src/api/errors.ts` — ~47 захардкоженных русских строк (26 human + 20 hint + `'Неизвестная ошибка'`) в обход типизированного i18n, при том что приложение поддерживает ru/kk/en: юзер с en/kk видит happy-path переведённым, а **весь unhappy-path — по-русски**. Из них 4 подсказки (`:29,:76,:83,:90`) ещё и мертвы после cloud-cut — предлагают «переключиться на облако в Настройках». Там же `utils/callMeta.ts:22` `humanSpeakerLabel` («Голос N»/«Спикер N»/«Я»), `:120` `simpleDateTitle` («Звонок …») и `:145` плюрализация «участник/участника/участников» — рендерятся в транскрипте, карточках спикеров и заголовке страницы. Образец правильной интеграции рядом: `utils/modelLabel.ts:27` принимает `TFn`. Фикс: перевести PATTERNS на ключи + прокинуть `t` (правило 4), удалить облачные хинты, словари en/kk уже типизированы под это.
  - **Готово, тремя коммитами.** (1) Мёртвые облачные подсказки удалены отдельно — это изменение того, ЧТО видит пользователь, а не языка; заменены на выполнимые действия. (2) 28 паттернов `api/errors.ts` переведены на `TranslationKey`, `humanError` принимает `t`, ключи заведены в ru/kk/en; `t` прокинута по 71 call-site в 22 файлах, четыре хука получили `useI18n()`. (3) `callMeta.ts`: `humanSpeakerLabel`, `shortSpeakerLabel`, `simpleDateTitle` (плюс формат даты через существующий `bcp47(locale)` вместо прибитого `'ru-RU'`) и плюрализация на существующих ключах `participants.one/few/many`.
  - **Две ошибки по дороге, обе поймал инструмент.** При переписывании `errors.ts` целиком я потерял три паттерна (`local_engine_transcript_read`, `local_engine_stt_failed`, `local_engine_recap_persist`) — читал файл кусками и пропустил середину; поймали тесты, список регулярок затем сверен с git построчно. В `PermissionsSection` локальный параметр назывался `t: Target` и затенял i18n-`t` — поймал tsc. Тесты `errors.test.ts` и `callMeta.test.ts` берут настоящий русский словарь, а не заглушку: так они проверяют реальные тексты и ловят отсутствующий ключ.
  - **Побочно** удалён мёртвый дубль `pluralParticipants` в `pages/CallDetailUtils.ts`. Обе копии, впрочем, никем не вызываются — функция переведена, но её судьба (удалять ли) относится к отдельному проходу по мёртвому коду.
- [x] **TD-26 InboxView-pass и фронт-мелочи** (M) — `apps/desktop/src/pages/InboxView.tsx:449` `refresh()` ставит error при падении `listCalls`, но **никогда не сбрасывает его при успехе**, а рендер (`:665`) ставит error-ветку первой: один transient (`database is locked`) → юзер видит текст ошибки вместо списка до перезапуска приложения. `:534` — N+1: `Promise.allSettled` с отдельным `listCallSpeakers(c.id)` на каждый ready-звонок при каждом refresh (инбокс всегда смонтирован через keep-alive, так что стреляет даже пока юзер в настройках) → нужна батч-команда на Rust-стороне или кэш по call_id. `:727-734` — мёртвая аффорданса сортировки: колонки «Длительность»/«Дата» имеют иконку sort и `cursor:pointer`, но ни `onClick`, ни sort-state не существует. `useInboxRowActions.ts:29` — reprocess из row-меню без подтверждения, тогда как та же операция на CallDetailPage требует confirm (`:204`), а `onRowDelete` рядом confirm имеет. Плюс `LocalEngineSection.tsx:260` читает `semanticSearch`, которого нет в deps `useCallback` (`:271`) → stale closure, e5-модели не встают в очередь после включения тумблера; `App.tsx:126,130` `getSetting(...).then(...)` без `.catch` → unhandled rejection на locked DB (соседний consent-вызов catch имеет).
  - **Готово 4 из 5.** Успешный refresh снимает ошибку (рендер держит error-ветку первой, поэтому один transient «database is locked» показывал текст ошибки вместо списка до перезапуска). Сортировка доведена до рабочей, а не убрана: `.th-sort` есть в каноне uikit, значит аффорданса задумана; при сортировке по длительности группировка по месяцам выключается — иначе «самый длинный» означало бы «самый длинный внутри своего месяца». Заголовки стали кнопками с `aria-sort`, сброс хрома — в `components.css` по образцу `button.chip` (`wk.css` не тронут, он порт канона). reprocess из row-меню получил то же подтверждение, что на CallDetailPage. `semanticSearch` добавлен в deps. Два `getSetting` получили `.catch`.
  - **Тест на снятие залипшей ошибки написан НЕ был, осознанно.** `refresh` вызывается из слушателей pipeline-событий, а `listen` в тест-файле замокан в noop — дёрнуть повторный успешный refresh на живом инстансе неоткуда. Через `rerender` с новым `key` тест выходит фиктивным: перемонтирование само сбрасывает состояние, и он зеленеет на сломанном коде. Я такой написал, увидел на откате, что он не падает, и удалил — причина записана прямо в тест-файле.
  - **Не сделано: N+1 по спикерам (`:534`).** Требует новой Rust-команды, то есть `cargo build`, а параллельно с работающим `pnpm tauri dev` это даёт OOM на 16 ГБ (см. память по проекту). Выделено в **TD-46**.
- [x] **TD-27 InteractiveTranscript: перф во время playback** (M) — `hooks/useCallAudio.ts:103,124` дёргает `setCurrentTime` на каждый `timeupdate` обоих `<audio>` (~4/сек каждый), state живёт на уровне `CallDetailPage` (`:727`) → каждый тик ре-рендерит всё поддерево: сотни `.turn`-строк для часового звонка, `CallRail`, `AudioScrubber`. В кодовой базе **ноль `React.memo`**, строки не мемоизированы и не виртуализированы, плюс smooth `scrollIntoView` на каждую смену активной реплики. Фикс: `memo(TurnRow)` с передачей только `isActive` (или CSS-подход через data-атрибут + один эффект), throttle `setCurrentTime` до ~1/сек. Проверка: React Profiler до/после на часовом звонке.
  - **Готово.** Реплика вынесена в мемоизированный `TurnRow` с примитивными props — на тик перерисовываются ровно две строки вместо всех. **Ключевая деталь:** `onSeek` приходит из `CallDetailPage` инлайновой стрелкой, то есть с новой ссылкой на каждый рендер; без стабилизации через ref `memo` не сработал бы вовсе и вынос строки дал бы ровно ноль. Плюс автоскролл уважает `prefers-reduced-motion` — плавный скролл запускался на каждой смене реплики, то есть при воспроизведении почти непрерывно (проверено откатом).
  - **Отступление от формулировки:** throttle `setCurrentTime` до ~1/сек не сделан намеренно — 1 Гц делает скраббер рваным, а корень был не в частоте тиков, а в том, что тик перерисовывал сотни строк. Добавить можно отдельно, но платить плавностью плеера без замера не стоит.
  - **Выигрыш не измерен:** задача предписывает React Profiler до/после на часовом звонке, headless его не прогнать. Структурная часть проверена tsc и 30 существующими тестами (поведение не изменилось).

## W6 · Дизайн-система и a11y

- [x] **TD-28 Self-host шрифтов** (S) — `apps/desktop/src/styles/fonts.css:9` тянет Hanken Grotesk и IBM Plex Mono `@import`'ом с Google Fonts CDN. Для local-first приложения это значит: без сети вся типографическая идентичность молча падает в `-apple-system`/`Menlo`, а каждый запуск шлёт запрос в Google — **fingerprint-утечка для продукта, позиционирующего приватность**. Файл сам содержит готовый закомментированный OPTION B (`:12-30`), `public/` в проекте пока нет. Фикс: положить woff2 в `apps/desktop/public/fonts/`, включить OPTION B, убрать CDN. Проверка: запуск с отключённой сетью — шрифты на месте.
  - **Готово.** 15 woff2 (139 КБ) в `apps/desktop/public/fonts/`, `fonts.css` сгенерирован из ответа Google с сохранением `unicode-range` каждого subset'а. Hanken взят вариативным (`wght@400..700`) — один файл на subset вместо семи начертаний; вьетнамский subset не скачан намеренно (локали ru/kk/en его не требуют, ещё ~40 КБ). Проверено живьём: dev-сервер отдаёт `/fonts/*.woff2` со статусом 200 и `content-type: font/woff2`, production-сборка кладёт их в `dist/fonts`, в CSS-бандле ноль обращений к googleapis. Провенанс и процедура обновления — `apps/desktop/public/fonts/README.md`.
  - **Всплыло по дороге: у Hanken Grotesk нет базовой кириллицы.** Google отдаёт для него только `cyrillic-ext` (U+0460–052F, историческая и расширенная); диапазона U+0400–045F — обычных русских и казахских букв — в шрифте нет. То есть весь русский и казахский интерфейс рендерится системным фолбэком, и так было всегда — self-hosting лишь сделал факт явным. IBM Plex Mono кириллицу имеет полностью, включая казахские Ұ/Ү. Решение продуктовое → **TD-47**.
- [x] **TD-29 A11y-pass примитивов** (M) — четыре связанных дыры в фундаменте: (a) ни один примитив `wk.css` (`.btn`/`.iconbtn`/`.navitem`/`.tab`/`.seg`/`.trow`/`.menu-item`/`.switch`) не имеет `:focus-visible` — фокус рисуется UA-дефолтом WebKit, при этом комментарий `global.css:1-3` **утверждает, что reset есть в wk.css** (его там ноль); стратегия фокуса получилась реактивной — 7 точечных классов, добавленных по итогам a11y-ревью отдельных фич; (b) `wk.css:193` `.switch` 34×20px при минимуме 24×24 WCAG 2.5.8, без hit-area и без focus-ring — худшее комбо для контрола, который чаще всего дёргают в настройках (паттерн расширения хитбокса `::after inset -3px` уже применён к `button.chip`, `components.css:1521`); (c) `tokens.css:58` `--text-faint` (#9C9FAB ≈ 2.6:1) покрашен не декор, а несущий текст 10.5px: `.tbl-head` (`wk.css:302`), `.sec-label` (`:221`), `.menu-label` (`:241`), `.turn-time` (`:327`), `.kbd` (`:161`) — проект сам признал проблему точечно в B24.7, но остальные ~15 мест остались; (d) `ui/Tabs.tsx:76` roving tabindex без `ArrowLeft/Right/Home/End` — Tab пропускает неактивные триггеры, стрелки не работают, **вкладки Транскрипт/Рекап/Ассистент недостижимы с клавиатуры** (`aria-controls`/`aria-selected` при этом сделаны правильно). Фикс: системный `:where(...):focus-visible` блок в wk.css + правка лживого комментария; хитбокс и ring для switch; текстовые роли на `--text-3`; клавиатурная навигация табов. Ревью — обязательно через агента `a11y-architect`.
  - **Готово, все четыре.** Системный `:focus-visible` в `wk.css` + исправлен лживый комментарий в `global.css`; хитбокс `.switch` расширен до 38×28 через `::before` (образец — `button.chip`); несущий текст переведён с `--text-faint` на `--text-2`/`--text-3` по фактическому фону; в табы добавлены `←/→/Home/End` с круговым обходом и пропуском `disabled` (проверено откатом — падают все три теста).
  - **Ревью `a11y-architect` нашло у меня настоящую ошибку.** `:focus-visible` стоял ВНЕ `:where()`, поэтому блок имел специфичность класса, а не нулевую, как утверждал мой комментарий: он выигрывал ничью по порядку исходника у `.btn`/`.iconbtn`/`.chip`, и радиус скакал с 6px на 4px на время фокуса. Псевдокласс внесён внутрь `:where()`, `border-radius` из блока убран (outline и так повторяет радиус элемента — явное значение как раз форму и ломало). Оттуда же: убран дубль `position: relative`, добавлен забытый `.th-sort`, и **пересчитан контраст по фактическим фонам** — `--text-3` на `--bg` даёт 4.47:1 и НЕ дотягивает до 4.5:1, поэтому `.tbl-head`/`.turn-time`/`.sec-label` ушли на `--text-2` (8.18:1).
  - **Не чинил:** sticky-шапка `.tbl-head` перекрывает верхние ~4px focus-ring первой строки `.trow`; кольцо видно с трёх сторон, лечится `scroll-padding` на контейнере — правка вёрстки списка, а не примитивов.
- [x] **TD-30 Скелетоны и CSS-гигиена** (S) — `components/call-detail/CallDetailSkeleton.tsx:54` рисует ghost-строки легаси-классами `.transcript-row` (grid `130px 1fr 60px`, текст 17px, `components.css:144`), а живой транскрипт — v2-классом `.turn` (grid `140px minmax(0,1fr)`, `--t-15`, `wk.css:324`): при загрузке→контенте меняются и сетка, и кегль — **layout-jump ровно там, где скелетон должен от него защищать**. Рядом три параллельные shimmer-системы (`.skeleton`+`sk` в `wk.css:268`, `.ds-skeleton`+`ds-skeleton-shimmer` в `ui/ui.css:11,28` — именно её использует `ui/Skeleton.tsx`, и `wt-skel-shimmer` в `components.css:24`), причём канонический uikit-`.skeleton` фактически мёртв, а шапка `ui/ui.css:2-3` ссылается на удалённый в B18.6 `styles/wotold.css`. Плюс `components/DualWaveform.tsx:95` canvas-фоллбек `'#ECEAE3'` — тёплый тон старой Atelier-гаммы, отсутствующий в v2-палитре (соседние фоллбеки `--text`/`--accent` синхронизированы корректно), в тёмной теме ещё и инвертирован неверно. Фикс: скелетон на `.turn`-разметку; одна shimmer-система; актуализировать шапку; фоллбеки в именованные константы со ссылкой на tokens.css.
  - **Готово.** Скелетон переведён на `.turn` — форма повторяет generating-состояние `InteractiveTranscript`, то есть правильный образец в проекте уже был. `ui/Skeleton.tsx` эмитит канонический `.skeleton`, система `.ds-skeleton` удалена вместе с keyframes и reduced-motion правилом. Шапка `ui/ui.css` переписана. Фолбэки `DualWaveform` вынесены в именованные константы; `'#ECEAE3'` (тёплый Atelier, в v2-палитре отсутствует, в тёмной теме давал светлую линию на тёмном фоне) заменён на значение `--border`. Тест на layout-jump проверен откатом.
  - **`wt-skel-shimmer` НЕ тронут:** он анимирует три app-специфичных элемента, а не скелетоны как таковые — сводить к канону нужно вместе с этими компонентами, отдельной правкой.
- [x] **TD-31 Inline-styles: бюджет и ratchet** (L, фоновая) — токен-дисциплина по цвету реально enforced хуком, а по типографике и spacing — нет: в `apps/desktop/src/**/*.tsx` **170 сырых `fontSize:`** и **610 инлайн-`style={{`** (в одних `pages/` — 87 и 345), то есть шкалы `--t-11..28` и `--s1..9` фактически advisory. Это не разовый рефактор: зафиксировать текущие числа скриптом-счётчиком, запретить рост (ratchet в hook или CI), сводить к токенам по мере касания файлов. Сюда же долг `components.css` — 1671 строка append-only лога без секционного оглавления.
  - **Готово (ratchet поставлен, сведение — фоновое).** Замер: **170** `fontSize` и **612** `style={{` в 109 продуктовых `.tsx` (тесты не считаются). Аудит называл 610 — разница в два это мои же правки сессии. `scripts/inline-style-budget.mjs` с зафиксированной планкой, гейт в CI рядом с typecheck; планка опускается сама (`--update`) при уменьшении, расти запрещено. Проверено: добавление одного `style={{ fontSize }}` роняет `--check` с exit=1, откат возвращает ноль.
  - **Оглавление `components.css` — без номеров строк намеренно.** Первую версию с номерами написал и выбросил: они разъехались от самой вставки. Протухший указатель хуже отсутствующего — ровно этот класс дефектов чинился всю волну (шапка `ui/ui.css` несколько волн ссылалась на удалённый `styles/wotold.css`, комментарий `global.css` утверждал наличие focus-reset, которого не было).

## W7 · Тесты и инфраструктура

- [x] **TD-32 Убрать реальные sleep из тестов** (M) — синхронизация с фоновыми задачами через wall-clock: 16 sleep'ов в `pipeline/chunk_orchestrator.rs` (30-300ms: `:487,536,591,641,711,758,789,833,841,852,893,896,901,937,949,960`), 9 в `pipeline/resource_queue.rs` (в т.ч. `:422`), `audio/call_detect.rs:337`, и `db/calls/lifecycle.rs:850` — целых **1100ms ради секундной гранулярности rfc3339**. Под нагруженным CI-раннером 30-50ms может не хватить (флаки), плюс ~3с к каждому прогону. Правильный паттерн в кодовой базе уже есть и не обобщён: `providers/transcription/retry.rs:82` инжектирует sleep-функцию. Фикс: сигналить из тестового `enqueue_fn` через `Notify`/oneshot, либо `tokio::time::pause()`+`advance()` где логика тайм-базирована; для lifecycle — вставлять фиксированные `started_at` вместо ожидания (правило 6). Проверка: время `cargo test` падает, повторные прогоны стабильны.
  - **Сделана часть: `db/calls/lifecycle.rs` (5 ожиданий, ~1.67 с).** Самое дорогое — 1100 мс ради смены секунды в rfc3339 — заменено явным `UPDATE started_at`: тест про сортировку, а не про часы (1.1 с → 0.05 с). Четыре теста про накопление паузы получили хелпер `backdate_pause`, отодвигающий `paused_at` в прошлое; заодно ассерты стали точными — раньше в них закладывался запас на CI-jitter (≥100 при ожидаемых 150, ≥180 при 240). Подлинность проверена откатом.
  - **Замер поправил оценку задачи.** Аудит оценивал накладные в «~3 с к каждому прогону», но `chunk_orchestrator` (14 тестов, 16 ожиданий) укладывается в **0.43 с**, а `resource_queue` (7 тестов, 9 ожиданий) — в **0.08 с**: тесты идут параллельно, и сумма длительностей не равна настенному времени. То есть по времени там почти нечего выигрывать; настоящий риск — именно флаки на нагруженном раннере, а он замеру на этой машине не поддаётся.
  - **Осталось: 25 ожиданий в `pipeline/chunk_orchestrator.rs` (16) и `pipeline/resource_queue.rs` (9).** Все — синхронизация с фоновой задачей, каждое требует своего сигнала (`Notify`/oneshot) или `tokio::time::pause()`; это не механическая замена, а переделка тестового харнесса обоих файлов. Выделено в **TD-48**, чтобы не смешивать с уже сделанным.
  - **Решение владельца (2026-07-27) по остатку:** `thanks for watching` оставить в безусловном списке. Тюнинг фильтра — отдельная работа, которой ещё предстоит заняться специально; сейчас не размениваться. Риск принят и известен: если в звонке эту фразу произнесут всерьёз, она молча пропадёт. Снять просто — убрать строку из `HALLUCINATIONS_EXACT` в `local_engine/hallucination.rs`, она тогда попадёт под общий порог уверенности.

- [x] **TD-33 Glue-тесты оркестраций** (M) → до TD-35 — тестовая пирамида инвертирована ровно там, где ломалось: листья покрыты параноидально (`audio_merger`, `chunk_recovery`, db-репозитории), а центральная оркестрация живёт на ручных live-запусках. `commands/recording.rs:1162` `recover_chunked_call` / `:1182` `spawn_recover_chunked` — **ноль тестов** (`rg 'recover'` в тест-модуле пуст), при том что все три прод-бага M13 (chunk-0 path mismatch, дропнутый финальный chunk, halt-before-merge) были именно в этом клее; `run_local_inner` и headless-триггер `WOTOLD_RECOVER_CALL_ID` — тоже. Фикс: вынести цикл «(re)transcribe chunks → merge» в функцию, принимающую `run_chunk` замыканием (как уже сделано для `rotate_fn`/`enqueue_fn` в оркестраторе), покрыть: все done → сразу merge; частичный fail → failed; reconstruct error → ранний выход (правило 1). Дубль с [`ROADMAP.md`](ROADMAP.md) §B «`pipeline::run` / `reprocess_call` / `regenerate_recap` unit-тесты» — закрывать вместе, отметить в обоих файлах.
  - **Сделана часть: цикл STT и решение о finalize.** Вынесены в `pipeline/recovery_flow.rs` с инжектируемым раннером (приём тот же, что `rotate_fn`/`enqueue_fn` в оркестраторе). Четыре теста: happy-path с проверкой порядка чанков, падение одного чанка не прерывает остальные, полный провал, пустой список. Подлинность проверена откатом — возврат раннего выхода роняет два теста.
  - **Побочно закрыта настоящая дыра, а не только тестовая.** При провале ВСЕХ чанков код всё равно шёл в finalize: merge собирал пустой транскрипт и перетирал им то, что лежит на диске. Теперь `should_finalize` это блокирует с явным логом. Пустой список чанков при этом finalize НЕ блокирует — «нечего расшифровывать» ≠ «нечего собирать», и именно этот путь чинил halt-before-merge.
  - **Остаток закрыт: весь клей восстановления, а не только цикл STT.** `run_recovery_flow` (`pipeline/recovery_flow.rs`) принимает все три шага замыканиями — реконструкцию, раннер чанка и finalize, — поэтому проверяемо главное: **finalize не вызывается** ни после провала реконструкции, ни когда не расшифровался ни один чанк (merge собрал бы пустоту и перетёр диск). Интеграционный харнесс не понадобился: инжектировать оказалось дешевле, чем поднимать пул + стор + `AppHandle`. Пять тестов; подлинность проверена откатом обоих ранних выходов — падают ровно два.
  - **Отбор кандидатов авто-восстановления (B28.2) тоже был без тестов.** Пять условий, каждое из которых при ошибке либо трогает чужой звонок, либо зацикливает recovery, жили внутри цикла с `AppHandle` и диском. Вынесены в чистую `plan_auto_recovery`; восемь тестов. Первая версия теста «лимит важнее всего» оказалась ложно-зелёной (кандидат не был headless-target — перестановка условий её не роняла) — переписана, откат порядка теперь ловится.
  - **Побочно: два файла сверх лимита 800 стали меньше.** `commands/recording.rs` 1677 → 1426 (recovery уехал в `commands/recovery.rs`), `pipeline/mod.rs` 2444 → 1912 (тест-модуль вынесен в `pipeline/tests.rs`; дочерний модуль в отдельном файле видит приватные элементы родителя так же). Это было не украшательство, а условие работы: гейт TD-03 справедливо не давал дописать в них ни строки — именно там и не хватало тестов. TD-41/TD-35 остаются: оба файла всё ещё сверх лимита.
  - **Закрыт и дубль из [`ROADMAP.md`](ROADMAP.md) §B** — `reprocess_call` halt-gate (провальный чанк → ранний выход, причина в `failed_reason` И `recap_failed_reason`, звонок НЕ уезжает в `processing`) плюс зеркало на целых чанках; `regenerate_recap` — NotFound и отсутствующий transcript.md с сохранением прошлой причины; `pipeline::run` — причина провала обязана лечь в строку, а не только статус. Откат halt-гейта и перенос pre-clear роняют по тесту каждый.
- [x] **TD-34 Coverage ratchet фронта** (M) — `apps/desktop/vitest.config.ts:25-30` порог lines/statements/functions = **10%** при 599 живых тестах: можно удалить 80% тестов, и CI останется зелёным. Честнее фейковых 80% (Rust-гейт реальный — `ci.yml:206` `--fail-under-lines 50`), но ratchet ручной и с мая не двигался. Крупнейшие непокрытые поверхности: `hooks/useCallDetail.ts` (~516 строк, центральный data-hook — его тесты писать вместе с TD-24), `hooks/useCallAudio.ts` (285 строк, 0 тестов), `OnboardingPage`, секции настроек. Фикс: снять фактический процент, поднять гейт до «факт − 3пп», повторять при каждом заметном приросте.
  - **Готово.** Замер при 601 тесте: lines/statements **72.12**, functions 61.59, branches 85.52 — то есть порог 10% позволял удалить четыре пятых тестов незаметно. Пороги подняты до 69/69/58/82. Проверено, что гейт ловит: удаление трёх тест-файлов роняет lines до 64.29% и валит проверку.

- [x] **TD-41 Декомпозиция файлов сверх лимита 800** (L) — обнаружено гейтом TD-03 в ходе TD-05: `lib.rs` — 877 строк, `commands/recording.rs` — **1641**, `local_engine/models.rs` — **903** (TD-10), все сверх лимита 800. **Дополнено по итогам TD-18/TD-43** — гейт заблокировал ещё четыре файла, и каждый пришлось обходить: `pipeline/mod.rs` **2461** (пересекается с TD-35, туда нельзя добавить даже строку `pub mod`), `db/calls/lifecycle.rs` **1398**, `pipeline/recap.rs` **1139**, `pipeline/chunk_runner.rs` **851** (там применено осознанное ручное отступление на +2 строки). Два файла в тех же задачах уже порезаны как побочный эффект: `db/calls/speakers.rs` 975→510 (выделен `calls/suggestions.rs`), `pipeline/summary_validator.rs` 787→640 (выделен `summary_validator/text_similarity.rs`) — образец того, как резать: по доменной границе, вместе с тестами, общие тест-хелперы наверх в `test_support`. Гейт теперь честно блокирует их рост, то есть любая следующая правка этих файлов требует ручного обхода — это работает как напоминание, но не как решение. `recording.rs` — вдобавок топ-1 по churn в аудите. Резать: `recording.rs` по фазам (start/stop/pause · chunked-setup · recovery · orchestrator-glue), `lib.rs` — вынести регистрацию команд и setup-хуки. Делать после TD-33 (glue-тесты) по той же причине, что и TD-35.
  - **По итогам TD-33 два файла уменьшились:** `commands/recording.rs` **1641 → 1426** (фаза recovery выделена в `commands/recovery.rs`), `pipeline/mod.rs` **2461 → 1912** (тест-модуль вынесен в `pipeline/tests.rs`). Оба реза сделаны не ради метрики: гейт не давал дописать в эти файлы тесты, которых там и не хватало.
  - **Готово. За лимитом остался ровно один файл — `pipeline/mod.rs` (1914), он же предмет TD-35.** Оговорка к формулировке задачи: аудит называл семь файлов, но гейт меряет весь репозиторий, и за лимитом их было **восемнадцать** — одиннадцать Rust и шесть TS. Закрыты все Rust-файлы, кроме `pipeline/mod.rs`.
  - **Резалось двумя приёмами, по причине переполнения.** Где переполняли тесты, а код был цельным (`chunk_orchestrator` 430 кода + 700 тестов, `assistant/mod.rs` 469 + 530), тесты вынесены соседним файлом: доменной границы внутри нет — одна машина состояний и один сценарный набор к `ask_core`, дробить код ради метрики было бы хуже. Где домен был виден — резался код вместе с тестами.
  - **Что выделено.** `lib.rs` 876 → 240 (`app_window.rs` — меню/трей/окна/AppKit, `app_startup.rs` — panic-хук и фоновые задачи старта). `commands/recording.rs` 1426 → 635 (`chunked_setup.rs`, `chunk_retry.rs`, `orphan_reconcile.rs`). `db/calls/lifecycle.rs` 1423 → 679 (`pause.rs`, `recap_meta.rs`, `sweep.rs`, `delete.rs`). `pipeline/recap.rs` 1135 → 708 (`recap_render.rs`). `local_engine/models.rs` 943 → 692 (`model_catalog.rs`). `local_engine/llm.rs` 1063 → 727 (`llm_json.rs`, `llm_prompt.rs`, запуск сайдкара — в `sidecar.rs`). `assistant/indexer.rs` 1087 → 644 (`passages.rs`). `db/contacts.rs` 899 → 509 (`contact_identifiers.rs`). `db/assistant.rs` 806 → 531 (`assistant_passages.rs`, фасад `pub(crate) use` сохранён — 20+ путей `db::assistant::…` переписывать было незачем). `pipeline/chunk_runner.rs` 852 → 743 (`chunk_lang.rs`).
  - **Остаток вынесен в TD-49** — шесть TS-файлов сверх лимита (три локали i18n, `CallDetailPage`, `InboxView`, плюс порог для `.tsx`). Правило 8 писалось про Rust-модули, для словарей переводов оно требует отдельного решения, а не механического реза.
  - **Проверка.** 866 тестов на каждом шаге, число не менялось ни разу; `fmt`/`clippy -D warnings` зелёные; живой запуск после переезда стартовой обвязки.

- [x] **TD-44 Потеря устройства на системной дорожке** (M) — выделено из TD-21e. `ProcessTapRecorder` не имеет `AVAudioEngine`, поэтому механизм TD-21e (уведомление + пересбор) к нему неприменим. Аналог — слушатель свойств Core Audio (`kAudioHardwarePropertyDefaultOutputDevice`, живость tap'а) плюс тот же `AudioStallDetector` с `leg: .system`: детектор уже написан и ничего специфичного для микрофона не знает. Событие `device_lost` уже несёт поле `leg`, Rust-сторона уже классифицирует его как NonFatal — то есть протокол и приёмник готовы, нужна только swift-проводка.
  - **Готово.** `ProcessTapRecorder` получил тот же `AudioStallDetector` с `leg: .system`, watchdog на 1 с и слушателя `kAudioHardwarePropertyDefaultOutputDevice`. Разделение ролей как у близнеца: слушатель — быстрый триггер пересбора, решение «сообщать пользователю» принимает детектор (устройство вывода меняется и без потери кадров, говорить о потере было бы враньём). Rust-сторона не тронута: она читает `leg` из события и уже логирует деградацию.
  - **Пересбор пришлось выделить, а не написать заново.** Цепочка tap → aggregate → IOProc сидела внутри `start()` одним куском; вынесена в `makeTapChain`/`attachIOProc`, чтобы восстановление собирало её теми же строками, не трогая WAV-writer — файл и накопленные байты остаются те же, меняется только источник кадров. Конвертер при пересборе обнуляется: новый tap может отдать другой входной формат, и переиспользование конвертера писало бы мусор в WAV.
  - **Текст события стал общим для обеих дорожек** (`WotoldAudioCore/DeviceEventText`, 3 теста). Раньше сообщение собиралось строкой прямо в `AudioRecorder`; вторая дорожка получила бы вторую формулировку того же явления — ровно то расхождение близнецов, про которое правило 2. Заодно закрыт `-0.0 с` в баннере: время считается вычитанием, и перепутанный порядок аргументов давал бы отрицательную длительность.
  - **Честная граница проверки.** Сам обрыв устройства воспроизвести автотестом нельзя — нужен живой tap и переключение железа. Проверено то, что проверяемо: детектор и текст — юнит-тестами (50 Swift-тестов), а **два ложных срабатывания — живым прогоном сайдкара**: 22 с обычной записи (663 КБ system) не дали ни одного `device_lost`, и пауза на 9 с — втрое больше порога тишины — тоже, потому что детектор знает про паузу. Оба сценария и были главным риском этой правки: watchdog, который кричит на каждой нормальной записи, хуже отсутствующего.

- [x] **TD-47 UI-шрифт без кириллицы** (M, продуктовое) — выяснилось в TD-28. У Hanken Grotesk, который канон объявляет основным UI/текстовым шрифтом, **нет базового кириллического диапазона** U+0400–045F: Google отдаёт для него только `cyrillic-ext` (U+0460–052F). Значит весь русский и казахский интерфейс — то есть основной язык продукта — набирается системным фолбэком `-apple-system`, а не заявленным шрифтом; латиница и цифры при этом настоящий Hanken, отчего в одной строке смешиваются две гарнитуры. Так было с B18, self-hosting лишь сделал факт проверяемым. Варианты: (a) сменить UI-шрифт на покрывающий кириллицу (Inter, Manrope, IBM Plex Sans — последний уже частично в проекте); (b) оставить как есть и зафиксировать фолбэк в каноне явно, чтобы дизайн-ревью не считало это багом. **Решение владельца (2026-07-27): искать замену, критерий — «похож на Hanken либо стилистически безупречен».**

Факт, от которого отталкиваться: `--font` = `'Hanken Grotesk', -apple-system, …`, то есть кириллицу сегодня рисует **SF Pro** — системный шрифт macOS. Это не «сломано», это хорошая гротеска с полной кириллицей; плохо ровно одно — латиница и кириллица в одной строке из разных гарнитур.

Проверено (Google Fonts CSS): базовую кириллицу U+0400–045F **и** казахские Ұ/Ү (U+04B0–04B1) покрывают все четыре кандидата — **Inter**, **Manrope**, **Onest**, **Golos Text**.

Короткий список с обоснованием:
- **Inter** — безопасный выбор: спроектирован как UI-гротеска, близкая к SF Pro, то есть кириллица визуально не прыгнет относительно того, что пользователи видят сейчас. Характер нейтральнее Hanken.
- **Onest** — кириллица-first геометрическая гротеска, по темпераменту ближе всего к Hanken (открытые апертуры, крупный x-height).
- **Manrope** — геометрическая, узнаваемая; ближе к Hanken по «тёплости», но с собственным характером, который заметно изменит тон интерфейса.
- **Golos Text** — кириллица-first, спокойная и очень читаемая на мелких кеглях, что важно при обилии 10.5px в этой системе.

**Не менял вслепую:** подмена основной гарнитуры — самое заметное изменение в продукте, а сравнить их я могу только по метаданным, не глазами. Правильный следующий шаг — сравнительная страница в `DesignSystemPage` с реальными экранами (транскрипт, инбокс, настройки) в трёх кандидатах, светлая и тёмная тема. Сам свап после выбора — одна строка в `tokens.css` плюс скачивание woff2 по процедуре из `public/fonts/README.md`.

  - **Готово: Onest.** Сначала по критерию «шире покрытие» был поставлен Manrope (добавлял greek и vietnamese), но владельцу он не подошёл по виду — взят Onest, второй кандидат шорт-листа: кириллица-first геометрическая гротеска, ближе всего к прежнему Hanken по темпераменту. Покрытие уже — `cyrillic`, `cyrillic-ext`, `latin`, `latin-ext`; **договорённость: расширять по мере появления самих языков в продукте, а не заранее.** Набор при этом легче всех вариантов: 63 КБ против 73 у Manrope и 139 у исходного Hanken.
  - Обновлены токен, `fonts.css`, подпись в `DesignSystemPage`, канон `docs/design/wotold-v2/README.md` и `public/fonts/README.md`. Проверено живьём: `/fonts/onest-*.woff2` отдаются как `font/woff2`.
  - **Ловушка при проверке, стоит помнить:** запрос удалённого шрифта у Vite возвращает **200**, а не 404 — это SPA-фолбэк, отдающий `index.html`. Проверять надо `content-type`, иначе легко решить, что старые файлы остались.

- [x] **TD-48 Sleep в тестах оркестратора и очереди ресурсов** (M) — остаток TD-32. 16 ожиданий в `pipeline/chunk_orchestrator.rs` и 9 в `pipeline/resource_queue.rs`, все — синхронизация с фоновой задачей. По времени выигрыш мал (замер: 0.43 с и 0.08 с на файл, тесты идут параллельно), риск — флаки на нагруженном раннере. Каждое ожидание требует своего сигнала (`Notify`/oneshot) либо `tokio::time::pause()`+`advance()`; образец инжектируемого времени уже есть в `providers/transcription/`. Это переделка тестового харнесса обоих файлов, а не механическая замена.
  - **Готово, ожиданий по часам не осталось.** В оркестраторе — `CallProbe` (счётчик вызовов `rotate_fn`/`enqueue_fn` поверх `watch`): тест ждёт «оркестратор дошёл до N-го вызова», а не «прошло 50 мс». В очереди ресурсов — `wait_until` по наблюдаемому состоянию снапшота (кто держит ресурс, кто ждёт, сколько снапшотов эмитнуто), плюс `Notify` вместо «держим 30 мс, чтобы снапшот успел». Единственный оставшийся `sleep` — `std::thread::sleep` **внутри** `spawn_blocking`: он и есть предмет теста (задача держит permit), а не синхронизация.
  - **Утверждения «ничего не произошло» остались ограниченным ожиданием — и это честно.** Сигнала на отсутствие события не существует; такие места оформлены `expect_none_within` с явным намерением вместо голого sleep.
  - **Побочно вскрылась зависимость сценария от sleep'а.** `pause_freezes_chunk_elapsed` работал только потому, что 30 мс успевали пропустить RMS-сэмплы вперёд команды паузы. Убрали sleep — тест перестал резать чанк вовсе: момент начала паузы фиксировался по нулевому таймкоду, накопленная пауза выходила втрое больше реальной. Починено барьером на встречном давлении канала (`send_rms_settled`, ёмкость 1) — теперь порядок «сэмплы доехали → пауза» гарантирован, а не удачен.
  - **Замер и подлинность.** `chunk_orchestrator` 0.43 с → **0.26 с**, `resource_queue` 0.08 с → **0.09 с** (тут выигрыш и не ожидался — там дело было в риске, а не во времени); три прогона подряд стабильны. Проверено откатом: обнуление `capped_paused` в оркестраторе роняет `pause_freezes_chunk_elapsed`.

- [x] **TD-46 Батч-команда «спикеры по списку звонков»** (M) — выделено из TD-26. `InboxView.tsx:534` на каждый refresh делает `Promise.allSettled` с отдельным `listCallSpeakers(c.id)` на КАЖДЫЙ ready-звонок; инбокс смонтирован через keep-alive, поэтому стреляет даже пока пользователь в настройках. Нужна одна Rust-команда, отдающая спикеров пачкой (`WHERE call_id IN (…)`), либо кэш по `call_id` с инвалидацией на `pipeline:finished`. Не сделано в TD-26 по прозаичной причине: правка требует `cargo build`, а параллельно с работающим `pnpm tauri dev` это даёт OOM на 16 ГБ.
  - **Готово.** `db::list_speakers_for_calls` — один `IN (…)` с плейсхолдерами и разбиением по 300 id (лимит параметров SQLite; без него длинная история просто получала бы ошибку). Tauri-команда `list_call_speakers_batch` валидирует каждый id как UUID до запроса (правило 7): запрос параметризован, но невалидный id здесь означает ошибку вызывающего, и молча отдавать по нему пустоту — прятать её.
  - **Звонок без спикеров в ответе отсутствует**, а не приходит пустым массивом: пустой вектор для несуществующего id — это утверждение, что такой звонок есть.
  - **Близнец починен вместе** (правило 2): `ContactsPage.tsx` делал ровно тот же `Promise.allSettled` с запросом на каждый звонок — переведён на тот же батч.
  - **Тесты.** Rust: группировка по звонкам, отсутствие несуществующих id, поле-в-поле совпадение с поштучным запросом (инбокс рисует по этим данным аватары), разбиение сверх лимита параметров. Фронт: батч вызывается один раз со всеми готовыми id и `list_call_speakers` не зовётся вовсе; пустой список готовых звонков вообще не ходит в бэкенд. Подлинность проверена откатом фильтра `status === 'ready'` — второй тест падает.

- [x] **TD-42 Пагинация `list_calls`** (M) — выделено из TD-17. `db/calls/lifecycle.rs` `list_calls` тянет все 24 колонки всех строк без `LIMIT` на каждый рендер и каждое событие пайплайна. Радиус — 27 callsite'ов: Tauri-команда (`commands/calls.rs`), TS-обёртка (`api/recording.ts`), три страницы (`App.tsx` — самый горячий, рефетчит на **каждый** `pipeline:finished` и отбрасывает всё после `.slice(0,50)`; `InboxView`, `ContactsPage`), плюс моки и тесты. `started_at` уже проиндексирован (`0001_initial.sql:50`), но без LIMIT индекс не помогает. Сюда же: `commands/pipeline.rs` `regenerate_empty_recaps` грузит все звонки и фильтрует `status != "ready"` в Rust — просится в WHERE. Не горит: у владельца 13 звонков.
  - **Горячий путь закрыт.** `list_calls_page(limit, offset)` + `count_calls`; рельса «Недавние» (`App.tsx`) берёт пятьдесят строк и счётчик, а не всю историю на КАЖДОЕ `pipeline:finished` с последующим `.slice(0,50)`. Список колонок вынесен в одну константу — иначе пагинированный запрос со временем разъедется с полным по набору полей.
  - **Клэмп не косметический:** отрицательный `LIMIT` в SQLite означает «без ограничения», то есть ровно то, от чего задача уводит. Тест на это падает при снятии клэмпа.
  - **`regenerate_empty_recaps` переведён на `list_ready_call_ids`** — фильтр по статусу ушёл в WHERE, и вместо всех строк со всеми 24 колонками бульк-реген получает только id готовых.
  - **Граница проведена сознательно.** Инбокс и страница контактов по-прежнему берут полную историю, и это не недоделка: инбокс — витрина всей истории с фасетами и клиентской сортировкой, контакты агрегируют по всем звонкам. Их пагинация — не разгрузка запроса, а другой UX (бесконечная лента либо серверные фильтры и агрегаты), то есть продуктовое решение, а не техдолг. Если оно будет принято — задача переоткрывается именно под него.
  - **Тесты:** окно и порядок совпадают с полной выборкой, хвост короче страницы, клэмп отрицательных значений, `processing_via` деривится так же, как в полном списке (иначе рельса и инбокс показывали бы разные бейджи), счётчик считает все статусы, ready-id фильтруются в SQL.

- [x] **TD-43 Молчаливые no-op'ы в сеттерах спикеров** (S) — заведена задним числом: найдено в ходе разведки по TD-16/TD-17, в план тогда не попало. `db/calls/speakers.rs` `set_call_speaker_suggestion` и `db/calls/clusters.rs` `set_call_speaker_cluster` не проверяли `rows_affected`: при отсутствующей строке `(call_id, speaker_tag)` UPDATE — no-op, возвращающий `Ok`. Потерянный `cluster_embedding` = спикер без биометрии, и никто не узнаёт. Оба живые (`pipeline/mod.rs`). Плюс `#[allow(dead_code)] // wired в B3.4` висел на функции, которая давно вызывается из прода, — глушил настоящий dead-code-анализ.
  - **Готово:** `warn` с `call_id`/`speaker_tag` в обоих (правило 3 — деградация видима). **Не `Err`:** оба зовутся из пайплайна через `if let Err(e) { warn }`, контракт уже мягкий, менять семантику ради диагностики нечестно. Близнецы починены вместе (правило 2). Стухший `allow(dead_code)` снят. **Честная граница тестов:** они фиксируют контракт (не `Err`, фантомной строки нет) и позитивную половину (на существующей строке UPDATE реально пишет) — сам warn не проверяется, поймать лог-строку без глобального logger-харнеса нельзя, а ставить его ради этого не стали. **Ещё два стухших `#[allow(dead_code)]`** замечены рядом и НЕ тронуты: на `dedup_items` и `jaccard_token_overlap` в `summary_validator.rs` — оба зовутся из `recap.rs`; там же module-wide `#![allow(dead_code)]`, который стоит снимать целиком отдельным заходом.

- [x] **TD-49 Лимит 800 для фронтенда** (M) — выделено из TD-41: гейт TD-03 меряет весь репозиторий, и после закрытия Rust-части сверх лимита остаются шесть TS-файлов. Два — обычная декомпозиция: `pages/CallDetailPage.tsx` (844) и `pages/InboxView.tsx` (820), режутся по секциям экрана. Четыре — вопрос политики: `i18n/{ru,en,kk}.ts` (1148/1061/1059) — это словари, а не модули; «много маленьких файлов» для них означает разбиение по неймспейсам (`recording`, `assistant`, `settings`…), что удорожает добавление строки сразу в трёх локалях и легко расходится между ними. Решить: либо неймспейсы, либо явное исключение локалей из гейта с записью в CLAUDE.md рядом с правилом 8. Механически резать словарь пополам ради метрики — худший из вариантов.
  - **Решение владельца (2026-07-27): локали исключены из гейта.** Правило 8 писалось про модули, а локаль — плоская таблица строк с одной когезией по определению; неймспейсы означали бы три правки в трёх файлах за каждую новую строку и расхождения между локалями. Исключение прописано в `scripts/hooks/pre-write.mjs` и в CLAUDE.md рядом с самим правилом — гейт, который блокирует то, что чинить не собираются, просто врёт.
  - **Две страницы порезаны обычной декомпозицией.** `InboxView.tsx` 820 → 541: омни-бар (ввод, фасет-токены, меню) — самодостаточный контрол над `Facets`, списка звонков он не знает; `FacetDef` поднят в `inboxData`, раз стал общим. `CallDetailPage.tsx` 844 → 679: команды над звонком (переобработка, отмена, регенерации, экспорт, удаление) со своими busy-флагами уехали в `useCallDetailActions` по образцу существующего `useInboxRowActions`.
  - **Побочно закрыт свой же долг:** правки TD-42 и TD-48 сами вытолкнули за лимит `db/calls/lifecycle.rs` (836) и тесты оркестратора (824) — пагинация выделена в `calls/paging.rs`, пробы тестов в `chunk_orchestrator/probes.rs`.
  - **Итог:** сверх лимита остался ровно один файл — `pipeline/mod.rs` (1914), предмет TD-35. Разметка и логика перенесены символ-в-символ, 603 фронтовых теста и 875 Rust-тестов зелёные.

## W8 · Рефакторинги

> Только после точечных фиксов W3 и появления glue-тестов TD-33: декомпозиция без страховки — это способ воспроизвести класс багов M13.

- [ ] **TD-35 Декомпозиция pipeline/mod.rs и run_local_inner** (L) → блокировка снята, TD-33 закрыта — `apps/desktop/src-tauri/src/pipeline/mod.rs` после cloud-cut был ~2400 строк, после выноса тест-модуля в TD-33 — **1912**, из них `run_local_inner` (`:882-1367`) — монолит на ~485 строк: preset resolve, model check, chunked gate, audio merge, full-file STT, language re-pin, диаризация двух дорожек, merge, cluster, auto-bind, recap. Периферия декомпозирована отлично (35 модулей) — в корне остались именно маршруты и их хелперы. Резать по фазам (prepare / stt / diarize / cluster / recap / finalize) под 800-строчный лимит (правило 8). Точечные фиксы W3 делаются ДО: они S/M и верифицируемы на месте, декомпозиция потом двигает уже корректный код.
  - **Сделана половина: файл под лимитом (1914 → 783).** `local_llm.rs` — провайдер локальной модели, прогрев, резидентный сервер, регенерация рекапа (всё про то, как поднять и погасить модель); `diarize.rs` — диаризация дорожек со своими моделями и правилами деградации; `local_run.rs` — сам локальный маршрут. В `mod.rs` остались маршруты и их обвязка. Фасад сохранён: пути `crate::pipeline::…` снаружи не менялись, переписывать 20+ вызовов ради переезда файлов незачем. Сверх лимита в репозитории не осталось ни одного файла, кроме словарей переводов (исключены в TD-49).
  - **Осталось: сам `run_local_inner`** — монолит на ~490 строк внутри `local_run.rs` (файл 549, то есть под лимитом, но функция нет). Резать по фазам: prepare (preset + модели + upload-stage), stt (chunk-gate, merge аудио, транскрипты, пин языка), diarize, cluster, recap, finalize. Через границы фаз ходит много локалей — нужен явный тип на то, что передаётся, иначе получится набор функций с восемью аргументами.
  - **Живой прогон сделан (2026-07-27), маршрут цел.** С разрешения владельца заведён одноразовый звонок с синтезированной речью (`say` → 16 кГц mono WAV, две реплики) и прогнан headless-триггером `WOTOLD_RECOVER_CALL_ID` через весь локальный путь. Результат: `status=ready`, `summary_engine=local-qwen-3b`, `call_type=status_update`, транскрипт с двумя дорожками на одном таймлайне (`owner` + `speaker:1`), рекап с ключевыми пунктами и задачами, следом индексация ассистента с эмбеддингами. Тестовый звонок удалён каскадом; `PRAGMA integrity_check` — ok, сирот по `assistant_passages`/`voice_samples` не осталось.
  - **Первая попытка прогона провалилась, и это оказалось полезно.** Аудио положили в корень звонка, а восстановление сканирует `chunks/<idx>/` — `reconstruct` честно отказался, и finalize не запустился: ровно тот инвариант, который TD-33 и вводил, сработал на живом запуске, а не только в тесте.
  - **Осталось: сам `run_local_inner`** — монолит на ~490 строк внутри `local_run.rs` (файл 549, то есть под лимитом, но функция нет). Резать по фазам: prepare (preset + модели + upload-stage), stt (chunk-gate, merge аудио, транскрипты, пин языка), diarize, cluster, recap, finalize. Через границы фаз ходит много локалей — нужен явный тип на то, что передаётся, иначе получится набор функций с восемью аргументами.
- [ ] **TD-36 Дедуп local-recap и единый парсер настроек** (M) — локальный recap-блок продублирован трижды: в `run_local_inner` (`pipeline/mod.rs:1296` окрестности), в `regenerate_recap_local` (`:733-794`) и через `build_local_llm_provider` (`:528`), который существует ровно ради DRY, но которым `run_local_inner` не пользуется; `engine_label` match дословно повторён на `:794` и `:1296`. Комментарий в коде честно признаёт «отдельный backlog на унификацию» — каждый следующий фикс приходится вносить в 2-3 места, drift уже материализовался в истории коммитов. Параллельно настройки читаются двумя путями (типизированный `PipelineSettings::load` и сырые `db::get_setting` с дублированной parse-логикой), и **клэмпы уже разъехались**: `commands/recording.rs:49` использует `MAX_LOCAL_SPEAKERS` = 3 (`local_engine/diarization.rs:46`), а `pipeline/mod.rs:1112` хардкодит `1..=4`. Фикс: один `run_local_recap(...)` хелпер + один типизированный путь чтения настроек с общими константами клэмпов.
  - **Клэмп-дрейф закрыт — это был настоящий баг, а не дубль кода.** `mic_diarization_num_speakers` читали два места со своим разбором каждое: обвязка чанков клэмпила по `MAX_LOCAL_SPEAKERS` (=3), локальный маршрут хардкодил `1..=4`. То есть настройка «4 голоса» работала в одном пути записи и молча игнорировалась в другом. Теперь один разбор в `pipeline/settings.rs` (`read_num_speakers_override` + `mic_diarization_enabled`), ключи там же. Три теста; подлинность проверена откатом границы обратно на 4.
  - **Ярлык движка дедуплицирован.** `local_engine_label` был скопирован дословно в маршрут записи и в регенерацию — копия, которая молча устаревает: добавили бы четвёртый размер модели, и один путь показывал бы «local-qwen», другой честный ярлык. Вынесен чистой функцией, три теста (включая порядок веток: «1.5b» содержит «5b»).
  - **Осталось и требует живой проверки: сборка провайдера.** `build_local_llm_provider` и блок внутри `run_local_inner` разошлись не только формой — маршрут записи **не подключает резидентный llama-server** (`with_server`), а регенерация подключает; зато маршрут записи ставит `with_call` для очереди, а регенерация нет. Свести их — значит поменять поведение обоих путей (прогретая модель на записи, метка звонка в очереди на регенерации), поэтому это делается сессией, которая может прогнать живую запись, а не мимоходом.
- [x] **TD-37 Персистентные degraded-флаги на звонке** (M) — политика «degrade, don't die» проведена последовательно (фатален только STT owner-дорожки; диаризация, кластеры, auto-bind, recap, embeddings деградируют с warn), но **все degraded-состояния живут только в логах**: пользователь не отличает «system-трек ушёл в speaker:0» от «правда один голос», авторы сами это признают в диагностических комментариях. Фикс: набор флагов на звонке (таблица или JSON-колонка) + тихий индикатор в UI с расшифровкой; поле в контракт (S2 — правится в `packages/contracts`). Это инфраструктура правила 3 и потребитель TD-06 (recoverable rotate-ошибка → флаг вместо падения).
  - **Готово.** Миграция 0023 — колонка `calls.degraded_flags` (JSON-массив кодов). Таблицу не заводили сознательно: флаги читают всегда целиком и всегда для одного звонка, а писать их приходится из середины пайплайна, где лишний INSERT в связанную таблицу — ещё одна точка отказа на пути, который и так уже деградировал. NULL и `[]` значат одно и то же; старые звонки backfill'ом не трогали — этой информации о них просто нет.
  - **Запись идемпотентна и не фатальна.** Повтор флага не плодит дублей (пайплайн проходит по одной деградации дважды при retry чанка и reprocess), а ошибка записи логируется и не роняет звонок: падать из-за диагностики на уже деградировавшем пути хуже самой деградации. Битый JSON в колонке читается как «оговорок нет» — экран звонка важнее значка.
  - **Подключены три реальные точки** из тех, что раньше жили только в логе: частичный транскрипт (`ChunkGate::Partial`) и провал пере-расшифровки под пин языка на каждой из дорожек. Коды заведены и для диаризации/кластеров — их точки подключаются вместе с TD-45 и TD-06.
  - **Контракт S2** — `packages/contracts/src/degraded.ts` (union кодов), i18n во всех трёх локалях, чип в шапке звонка с расшифровкой в подсказке. Карта «код → ключ локали» явная, а не шаблонный ключ: код приходит из БД, и незнакомый (запись более новой версией) должен молча выпасть из подсказки, а не показать сырую строку.
  - **Сброс на reprocess** — до старта, а не после: иначе UI показывал бы прошлые оговорки поверх уже идущей переобработки.
  - **Тесты:** 5 на хранилище (накопление и порядок, идемпотентность, сброс, неизвестный звонок, битый JSON). Плюс непреднамеренная, но честная регрессия: два smoke-теста экрана звонка падают, если убрать защиту от `null` в ответе команды.
- [x] **TD-38 Единый WAV-reader** (S) — два модуля решают одну задачу (вырезать сегмент WAV в f32 для voice embedding) с **разными константами нормализации**: `apps/desktop/src-tauri/src/audio_io.rs:35` делит на `i16::MAX` (32767), `audio/wav_chunker.rs:82` — на 32768.0. Расхождение амплитуды ничтожно, но поведение уже разное (audio_io отвергает stereo, wav_chunker сворачивает в mono усреднением) — классический дубль, который разойдётся сильнее. Фикс: свести к одному модулю (wav_chunker полнее), зафиксировать одну константу, фикстурный тест.
  - **Готово:** Одна реализация-надмножество в `wav_chunker` (multi-channel + Int16 + **Float**, константа `I16_SCALE`), `audio_io::read_wav` делегирует туда и отвечает теперь только за нарезку по таймштампам. Константа — 32768, а не `i16::MAX`: диапазон i16 асимметричен (−32768..=32767), деление на 32767 давало **−1.00003** на минимуме, то есть выход за `[-1.0, 1.0]`; подлинность проверена откатом (`left: -1.0000305`). **Два намеренных изменения поведения:** (1) `read_wav` на stereo больше не `Err`, а усредняет каналы — так уже вёл себя ридер, который реально кормит кластеризацию, и для биометрии два канала полезнее ошибки; (2) амплитуда в `audio_io`-пути сдвинулась на 0.003% — значения идут в voice embedding, но cosine similarity инвариантна к масштабу, на матчинг не влияет. Ключевой тест — инвариант «оба публичных API дают те же сэмплы». 819 rust-тестов (+2 нетто).

- [x] **TD-45 Выравнивание дорожек после провала устройства** (M) — выделено из TD-21e. Дорожки сливаются в общий таймлайн по таймкодам внутри каждого WAV, поэтому дыра в mic.wav сдвигает **все** последующие mic-реплики относительно system на длительность провала. Замерено: 4.8 с провала = 4.8 с рассинхрона до конца чанка. Варианты: (a) дописывать тишину в дыру — выравнивает таймлайн, но фабрикует содержимое в файле, который пользователь считает записью разговора; (b) хранить список провалов на звонке и учитывать их при merge; (c) резать чанк по границе провала. **Решение владельца (2026-07-27): дописывать тишину — вариант (a)**, с оговоркой «если потерю никак не восполнить».
  - **Сделано ровно по решению.** `WAVWriter.writeSilence(seconds:)` пишет нули чанками по 16 KiB (провал в минуту на 16 кГц — это ~1.9 МБ, собирать их одним буфером незачем); оба рекордера зовут её на `.recovered(gapSec)` **до** того, как в файл пойдут новые кадры (правило 2 — близнецы правятся вместе).
  - **Оговорка владельца отработана буквально.** Мы действительно фабрикуем содержимое в файле, который пользователь считает записью разговора, поэтому событие обязано быть видимым: `device_recovered` с ненулевым провалом ставит degraded-флаг `mic_track_gap_padded` / `system_track_gap_padded` (инфраструктура TD-37), и он выводится чипом в шапке звонка с расшифровкой. Молча тишину не дописываем.
  - **Тесты:** длительность считается по частоте и каналам, тишина — действительно нули (а не мусор из памяти), нулевая/отрицательная/`NaN`/`inf` длительность ничего не пишет, длинный провал идёт чанками. 54 Swift-теста.
  - **Граница проверки та же, что у TD-44:** сам провал устройства автотестом не воспроизвести. Проверено, что нормальная запись не изменилась — живой прогон сайдкара (11 с, обе дорожки пишутся, событий провала нет).

Оговорка существенна и должна быть реализована буквально: тишина дописывается **только** когда провал уже случился и восстановить аудио неоткуда. Порядок такой — сначала попытка восстановления (TD-21e её делает: пересбор engine возвращает дорожку за ~0.5 с), и лишь если после этого в файле осталась дыра, она добивается нулевыми сэмплами до фактической длительности провала.

Обязательные условия, иначе фикс станет хуже болезни:
1. Дописанная тишина должна быть **видна** — счётчик секунд на звонке (инфраструктура TD-37), а не только в логе. Пользователь обязан знать, что часть записи — не «тихо», а «потеряно».
2. Длина пада берётся из измеренного `gap_sec` события `device_recovered`, а не из разницы таймстампов файла.
3. Пад не дописывается, если провал пришёлся на паузу (TD-07): там дыра намеренная и уже учтена в `paused_total_ms`.
4. Потолок RIFF (TD-21c) действует и здесь — пад идёт через тот же `appendBytes`.

- [ ] **TD-50 Осиротевшие каталоги звонков** (S) — замечено при уборке за живым прогоном TD-35: в `calls/` двадцать каталогов при тринадцати строках в БД. Семь лишних — от 2026-06-29, то есть переживают удаление звонка уже месяц. Аудио удалённого звонка остаётся на диске: это и место (часы записи), и приватность (C5 требует полной очистки). Проверить, какой путь удаления не сносит каталог (`delete_call_and_samples` чистит только БД, каталог — на вызывающем: `commands::calls::delete_call` зовёт `remove_call_dir`, а вот bulk-пути и sweep — вопрос), и добавить уборку сирот на старте по тому же принципу, что `reconcile_orphan_recordings`.

## W9 · Документация

- [x] **TD-39 Паспорт и QA-матрица** (S) — (a) `docs/ПАСПОРТ_ПРОЕКТА.md:345` §7-bis перечисляет пост-MVP майлстоуны только M12–M14: **M15 «Ассистент» и M16 «Recall» — крупнейший реализованный пласт продукта** (18 задач, миграции 0019–0021, отдельный PRD) — существуют только в ROADMAP/PRD; при правиле «паспорт побеждает» самая большая фича формально не имеет статуса в источнике истины. Дописать аддендумы по образцу M12–M14 + обновить статус-строку §11. (b) `CLAUDE.md:116` и [`ROADMAP.md`](ROADMAP.md)`:171` требуют QA «6 theme×accent (light/dark × bordeaux/persian/ink)», тогда как CLAUDE.md:94 фиксирует, что пикер убран в B18.5 и акцент один — графит: реальных комбинаций **2**. Агент, следующий инструкции буквально, тратит время на несуществующие режимы. Привести оба файла к «light + dark (акцент ink)».
  - **Готово.** (a) §7-bis получил аддендумы M15 (RAG-ассистент: гибридный retrieval, детерминированная привязка источников через `used_fragments`, защита промпта от инъекций через контент звонка — тот же принцип, что у MCP, но недоверенный текст идёт прямо в промпт локальной модели) и M16 (Recall: развилка extractive/summarize, ретрай с nudge, RU-стоплист в FTS-запросе); статус-строка §11 — «M12–M16». (b) QA-матрица в CLAUDE.md и ROADMAP приведена к «light + dark» с явной пометкой, почему прежние «6 theme×accent» были враньём: пикер акцентов убран в B18.5.
- [x] **TD-40 Док-мелочи** (S) — (a) `docs/design/wotold-v2/README.md:36-37` утверждает, что Atelier-имена «держатся через `styles/legacy-tokens.css` shim до B18.6», хотя shim удалён и файла нет; раздел «Статус миграции» застыл на B18.0→B18.6, хотя система уехала до B30, а ссылка ведёт на секцию, переехавшую в `ROADMAP_ARCHIVE.md` — для документа со статусом «канон» это прямое противоречие коду; дописать упоминание `components.css` как второго слоя и `assistant.md` как addendum. (b) MCP `find_calls_by_contact` принимает `contact_name` вместо `contact_id` из паспорта M8.2 (`services/mcp/src/tools.ts:186`) — прагматичное отклонение в пользу LLM-клиента, нигде не задокументированное; внести в [`MCP.md`](MCP.md) как deviation. (c) `packages/contracts/src/index.ts:1` `CONTRACTS_VERSION = '0.0.1'` не потребляется нигде и не бампался при добавлении summary_v2/local-engine/assistant — создаёт ложный сигнал о существующем механизме совместимости (реальное версионирование — per-schema литералы); удалить либо реально потреблять (handshake MCP).
  - **Готово.** (b) В `docs/MCP.md` заведён раздел «Отклонения от паспорта»: `find_calls_by_contact` берёт имя, а не id, потому что инструмент зовёт LLM-клиент — у него на руках расшифровка с именами, но без внутренних идентификаторов; требовать id значило бы перенести нечёткое сопоставление на худшую для этого сторону. Записан и побочный эффект: имя не уникально. (c) `CONTRACTS_VERSION` удалена, причина записана на её месте, чтобы не завели обратно «для порядка»; реальное версионирование в проекте пер-схемное.
  - **(a) оказался уже исправленным** более ранними правками: shim описан как удалённый, `components.css` и `assistant.md` упомянуты, ссылка ведёт в `ROADMAP_ARCHIVE.md`. Отдельной работы не потребовалось.

---

## Сводка

| Волна | Задач | Тема |
|---|---|---|
| W1 | 4 | Релизный workflow, SHA-пины, харнесс, вендоринг дизайна |
| W2 | 7 | Path traversal, пауза, таксономия ошибок, llm-server, целостность моделей |
| W3 | 10 | События пайплайна, гонки, FSM-гейты, spawn_blocking, Swift twin-parity |
| W4 | 2 | Роутер, retrieval, детерминизм ассистента |
| W5 | 4 | Ошибки страницы, i18n, инбокс, перф транскрипта |
| W6 | 4 | Шрифты, focus-visible/контраст/таргеты, скелетоны, inline-styles |
| W7 | 11 | Sleep в тестах, glue-тесты, coverage ratchet, декомпозиция, пагинация, сеттеры спикеров, системная дорожка, батч-спикеры, лимит 800 на фронте |
| W8 | 5 | Декомпозиция mod.rs, дедуп recap, degraded-флаги, WAV-reader, выравнивание после провала |
| W9 | 3 | Паспорт/QA-матрица, док-мелочи |

**Итого 50 задач** (55 находок аудита после бандлинга + 9 заведены по ходу работ: TD-41…TD-49). Effort: 18×S, 24×M, 6×L.

**Закрыто: 47/50.** W1 4/4, W2 7/7, **W3 10/10**, **W4 2/2**, **W5 4/4**, **W6 4/4**, **W7 11/11**, W8 3/5 (TD-38, TD-37, TD-45), **W9 2/2**.

**Release-критично (⚑):** TD-01, TD-02. Продуктовые блокеры релиза (LICENSE, consent-notice) — в [`ROADMAP.md`](ROADMAP.md) §A.
